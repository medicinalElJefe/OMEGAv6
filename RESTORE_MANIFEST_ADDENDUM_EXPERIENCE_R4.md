# OMEGA Restore / Surpass Addendum — Experience R4

## RELEASE / LINEAGE
- Local package lineage remains Drive `LATEST_OMEGA_UPDATE.json` → B015 R1. The pointer retains SHA-256 verification, immutable-core preflight, B007-B015 gates, crash watchdog, transactional rollback and activation evidence.
- Public-cloud promotion is additionally authorized by the owner on 2026-08-29 for exact tested restore-and-surpass candidates. This does not waive local release law, truth boundaries, tests, rollback or canonical public verification.
- Parent public `main` for this development turn: `d6dbb36e9a5b180ce64aabd500bf7334ce3309f0`.
- Active integration branch: `surpass-mainline`, rebased by construction on that main before R4 work.

## BROAD DONOR REVIEW USED
This turn did not stop at one visual donor. Fresh Drive search was combined with the existing full-review/index evidence in the ChatGPT file library.

### SVG / vector capability
Fresh Drive metadata confirms multiple `QtSvg`, `QtSvgWidgets`, and `svgLib` folder families. They are classified **SOURCE-BIND-REQUIRED**: they prove substantial vector capability exists, but unseen SVG geometry is not invented or copied blindly. R4 uses source-driven SVG/Canvas geometry now and leaves exact per-file SVG migration for bounded subsequent turns.

### Phase / time donor — ADAPTED
Drive donor `omega_visual_atlas_live.html` was inspected directly. It contains a 12-node live phase atlas, play/scrub timing, time traces and the canonical phase labels:
1. Initiation
2. Acceleration
3. Expansion
4. Momentum
5. Saturation
6. Constraint Engagement
7. Deceleration
8. Redistribution
9. Compression
10. Pre-turn Tension
11. Turn-Reorientation
12. Integration

R4 adapts this donor into `src/PhaseWheel.tsx`. Each wheel position maps to a real canonical address with the same D/R/L and selected P coordinate (`1728*d + 144*p + 12*r + l`). Selection therefore changes actual OMEGA state rather than applying decorative UI state. The wheel explicitly labels its animation as model timing, not external physical observation.

### Earth donor family — PARTIAL EXACT / SOURCE-BIND-REQUIRED
Fresh Drive search identified `20736D_Earth_Space_Motion_Atlas.xlsx`, `Earth_Moon_Resonance_Atlas.xlsx`, `earth_topology_real_dem_bathy_comparison.xlsx`, `Earth_12x12_Matrix.xlsx`, `Earth_Atmosphere_Packet_System.pdf`, `Earth_Geometric_Map_InsideOut.pdf`, and measurement-atlas material. Library architecture evidence specifies the Earth Traversal Engine as WGS84/GIS/evidence-gated and explicitly forbids fake terrain as fact.

R4 restores a materially richer Earth runtime using bundled Natural Earth 1:110m land geometry plus the existing analytical UTC solar/subsolar/terminator computation. It adds sidereal, sun-centered, source-centered and free geographic views plus explicit accelerated time traversal. External weather/satellite/traffic observation remains `EXTERNAL_DEGRADED`; the 20,736-state marker remains explicitly representational. Exact Drive Earth workbook ingestion remains **STILL MISSING / SOURCE-BIND-REQUIRED** and must be migrated only after schema/evidence review.

### Renderer / motion architecture — RETAINED
Library evidence reaffirms one packet substrate, motion-first continuity field, temporal accumulation, observer Now frame, state→field rendering, evidence overlays, and the rule that renderer output is not source authority. `MatterTraversal` remains the authoritative existing WebGL field implementation during R4; it is not replaced by a decorative animation. R4 adds shared phase/timing control and readability around it while deeper shader/palette calibration remains a bounded follow-up.

### Hybrid Link donor family — RETAINED / VISUALLY SURPASSED
Drive and library evidence confirms Hybrid Link v21 and adapter/transport contracts: filesystem, workbook, runtime, renderer, patch and websocket adapters; bridge-verify-return semantics; and `bridge not authority`. R4 retains `DEVICE_PROOF_REQUIRED` for native execution while reorganizing the browser mission surface around an explicit bridge topology and shared phase context.

## AI / COMMAND CENTER REPAIR
The deployed source showed why the assistant felt unoccupied: `/api/chat` returned `MODEL_PROVIDER_NOT_CONFIGURED` for synthesis unless an external endpoint secret existed. R4 repairs this without pretending model output is source truth:
- `wrangler.jsonc` now binds Cloudflare Workers AI as `env.AI`.
- Primary cloud synthesis model: `@cf/google/gemma-4-26b-a4b-it`.
- B020 route-before-generation remains first.
- The browser supplies a bounded current runtime packet: address/state, D/P/R/L, phase, metrics, PSC/predict state, next route, all-mode counts/top mode summary and unified projections.
- The model system contract forbids fabricated Earth/device/provider/physical-validation claims and distinguishes runtime state, model interpretation, external observation and unavailable evidence.
- If Workers AI fails, OMEGA returns an HTTP-success grounded deterministic fallback from supplied runtime context instead of appearing dead or fabricating synthesis.
- External model endpoint remains an optional secondary adapter, not a critical dependency.

## EXPERIENCE / VISUAL DELTA
- Home is rebuilt around a first-time-user living phase field, four clear starting journeys, one working assistant surface, one daily editorial lesson and an explicit truth strip.
- `ALL MODES` is forced active on initial workstation entry and actual mode count is surfaced.
- Command Center, Matter, Relativity, Earth and Hybrid receive the same source-bound phase/timing instrument so data acts on the interface instead of being isolated in tables.
- Right runtime inspector is visually demoted, narrowed and restyled to stop dominating the primary application; NEXUS is moved out of its collision path.
- Relativity gets motion cues around actual chain values rather than fabricated imagery.
- Earth globe becomes the dominant visual while analytical telemetry becomes supporting instrumentation.
- Hybrid gets an explicit live bridge topology presentation without claiming an authenticated PC.

## BOUNDARIES / STILL MISSING
- Exact per-file SVG donor migration remains source-bind-required.
- Exact Drive Earth atlas/DEM/bathymetry/time-series schema ingestion remains source-bind-required.
- Matter shader color semantics need a dedicated calibration turn against corpus variables, phase/time donor data and physical-vs-computational legend tests; R4 intentionally does not pretend the existing computational palette is literal material color.
- Native Hybrid keyboard/mouse/filesystem/process control still requires authenticated host proof.
- External Earth observations still require verified provider/data bindings.

## TEST / PROMOTION LAW
`tests/experience-r4-invariants.mjs` locks canonical 12-phase names, real D/P/R/L phase address mapping, ALL-mode default, route-before-generation, Workers AI binding/model, deterministic fallback, critical truth boundaries, shared phase context and R4 activation. It is part of `test:home` and therefore the full `npm run check` gate.
