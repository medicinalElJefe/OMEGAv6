# R291 · Full Post-R289 Convergence Closure

R291 is the single forward release candidate that consolidates the valid post-R289 work without introducing a competing runtime, route authority, Canon writer, proof writer, or deployment path.

## Integrated execution and proof

- Deep Archive Execution Convergence: the recovered archive Genome is carried into an explicit bounded recovery/execution layer with provenance, scar state, counterexample handling and deterministic receipt generation.
- Relational Skin Calculus: the R290 proof VM remains a symbolic/model-space executor. Its engineered structural comparator is explicitly identified as engineered rather than an archived physical/scientific formula. External scientific proof, physical-law authority and CanonState mutation remain false.
- Navigation closure: the canonical 44-route set is still derived/resolved by the inherited navigation authority. R289/R290 presentation adds twelve recovered master-menu filters, while ALL plus the six workspace filters remain real pointer-operable controls on desktop and mobile.
- Heavy Bio closure: the 241 analytical-channel workbench retains measurement authority zero. The comparison surface is explicitly read-only with a machine-bound zero measurement-authority delta and a substantive visible truth boundary. R282 remains the only scope-matched clinical-influence admission layer.
- Archive/RSC browser proof: the live Archive Census surface must mount the deep-recovery instrument and symbolic RSC lab, generate a deterministic SHA-256 PASS receipt for the bounded proof case, fail closed to UNPROVED when counterexamples are uncontrolled, keep translation denied on that hold, exercise recovery filters, remain contained on desktop/mobile, and emit no page errors.

## Preserved system law

R291 preserves 44 canonical product routes, the recovered twelve-menu presentation, the existing Water → Woven → Violet constrained carry semantics, WGS84/Earth/SAR source truth, and the address/resolution ladder 12 → 144 → 1,728 → 20,736 → 248,832 as representational/computational resolution rather than literal physical dimensions.

`NO NEW PHYSICAL PRIMITIVE` remains binding. Archive presence is not execution proof. Rendered geometry, visual polish or model-space transformation never creates an observation. Blade Geometry remains unpromoted until an authoritative operator law is recovered and proven through the existing admission path.

## Authority

R291 preserves the established R125/R141/R142/R146/R147/R240 separation and leaves canonical `ci.yml` as sole main-push production deployment authority. The R291 static closure is chained through the existing governed release gate. The Archive/RSC browser proof executes directly inside the existing R241 real-UI proof sequence before the exhaustive 44-route desktop/mobile sweep; no new workflow or deployment authority is introduced.

Promotion is valid only when the exact R291 head passes R170, R202, R210, R223, R237, R238, Cloud Bridge and R241, and the resulting two-parent production merge then passes the canonical main-push deployment and live served-SHA verification.