# OMEGA R126 Total Maximum Build — Candidate Release Receipt

## Candidate identity

Branch: `r126-total-maximum-full-build`

Purpose: integrate the strongest current OMEGA runtime, all preserved mode/canon authority, the full stateful swarm hierarchy, autonomic scope/rejoin, accuracy-first residual repair, governed self-build, federation, Hybrid/Sovereign truth, Earth/Optical specialists and the complete responsive operator surface without flattening or replacing proven inherited layers.

## Candidate additions

- `omegaMaximumRuntimeR126.ts`: one maximum mission compiler over source modes, canon lenses, corpus authority, living metrics, device tiers, execution scale, scopes, budgets, evidence, accuracy and proof.
- `OmegaMaximumCockpitR126.tsx` + responsive CSS: one maximum operator surface.
- recovered autonomic Durable Object, API, UI and proof contracts on top of current main.
- `OMEGA_SWARM_AUTONOMIC` migration under R126 lineage.
- published `omega-r126-total-maximum.json` runtime manifest.
- R126 focused invariant suite and complete release workflow.
- convergence stack retains R123 organism, R121 direct swarm and the prior continuity field instrument.

## Preserved numerical semantics

- source-mode catalog: 179 metadata authorities; execution remains input gated.
- canon/calculus lenses: 62 higher-order governance/expression lenses over the same packet.
- hierarchy: 1 → 12 → 144 → 1,728 → 20,736.
- representation: 248,832.
- virtual address capacity: 61,917,364,224.
- none of these numbers is promoted into a literal physical-dimension claim.

## Authority invariants

- `OMEGAv6` is the canonical admission authority.
- `EXECUTION_QUORUM_NOT_TRUTH` remains explicit.
- `AUTONOMIC_RECEIPT_NOT_CANON` remains explicit.
- `SWARM_RECEIPT_RETURNED_NOT_ADMITTED` remains explicit.
- `SELF_BUILD_CANDIDATE_NOT_ADMITTED` remains explicit.
- source catalog membership is not execution.
- PC ONLINE requires a current non-revoked online Hybrid device plus native-execution claim.
- solver, surface reachability, model synthesis, empirical evidence and canonical admission remain distinct states.

## Capacity governance

- explicit FULL = 1,728 cells.
- AUTO escalation ≤ 288 cells unless `allowFullAuto=true`.
- Workers AI ≤ 12 total calls per mission including reconvergence.
- Genesis and Optical machine budgets are separately bounded.
- no unlimited retries for paid/external provider work.

## Promotion gate

The candidate may be promoted only after all of the following are green:

1. R126 total maximum invariants;
2. recovered autonomic scope/cost/quorum/rejoin invariants;
3. R125 accuracy-first invariants;
4. R124 self-build invariants;
5. R123 organism and living-runtime invariants;
6. R122 computed-reality invariants;
7. R121 swarm and Earth-source invariants;
8. runtime JavaScript syntax checks;
9. complete inherited `npm run check`;
10. Wrangler R126 Durable Object dry-run;
11. existing late browser/mobile/full-resolution gates remain green;
12. branch is reconciled against current `main` without dropping newer admitted self-build/accuracy work;
13. governed merge preserves deployment lineage requirements;
14. canonical Cloudflare deploy succeeds;
15. live autonomic manifest, one-cell proof-bounded route and `/api/system/convergence` canonical-authority checks pass.

Until these gates pass, production remains the currently deployed main revision. This candidate receipt is not a production-live claim.
