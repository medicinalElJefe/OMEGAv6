# OMEGA R165 — Active Optical R153.2 Convergence

R165 removes an active-generation mismatch without deleting predecessor evidence.

## What changes

The canonical `omegav6` Worker continues to boot from `src/workerR116.js`. All established Durable Object names, assets, AI bindings, Genesis bindings and inherited runtime behavior remain intact. The production `OMEGA_OPTICAL_MACHINE` service binding advances from the R115 machine to the independently live-proven `omega-optical-machine-r1532` service.

R153.2 remains `SCREEN_ONLY`. Its adaptive external-search loop can rank, refine and prepare a full-wave handoff, but it cannot claim RCWA/FDTD/FEM execution, fabrication validation, physical measurement or Canon admission without separate authenticated receipts.

## Why this is an upgrade rather than a rewrite

R115 remains the machine-adapter truth foundation. R152 remains the operational optical convergence layer. R153.1 remains the external push layer. R153.2 remains the bounded adaptive-search layer. R165 changes which compatible machine generation receives *new active Optical SCREEN traffic* while retaining all predecessor source, tests and historical claims.

The canonical runtime root is deliberately not replaced. R164 and the current production architecture preserve the R116 Worker spine; R165 changes the peer binding and publishes current-generation truth around that spine.

## Current handoff law

`Genesis PROPOSE -> Optical R153.2 SCREEN -> Sovereign SOLVE -> OMEGA V6 ADMIT`

Each arrow requires its own receipt. Reachability is not write authority; write authority is not execution; execution is not verification; verification is not promotion.

## Vercel boundary

`https://omega-living-light-etching-private-woven2.vercel.app/` remains an external/protected human target until the exact Vercel project is exposed as writable and the same URL passes post-deploy proof. R165 does not infer Vercel write authority from HTTP reachability.

## Dimensional boundary

12, 144, 1,728, 20,736, 248,832 and larger 12^n values remain atlas/address/composition resolution shells. R165 makes no literal-physical-dimension claim.

## Promotion gate

R165 can promote only after:

1. R165 active-generation invariants pass.
2. R153.2, R153.1, R152 and R115 inherited optical invariants pass.
3. The full inherited production regression passes.
4. The canonical Wrangler configuration dry-runs with the R153.2 service binding.
5. After merge, the normal canonical exact-SHA deployment pipeline proves the current public runtime.

Historical predecessor files are not cleanup targets; they are proof lineage.
