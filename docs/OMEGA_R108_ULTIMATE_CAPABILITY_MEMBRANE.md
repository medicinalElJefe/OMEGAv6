# OMEGA R108 — Ultimate Capability Membrane

R108 makes the R107 source/calculus/mode/capability fabric operational at the workflow boundary without creating a second router, renderer or canonical state.

## Canonical correlation

`INTENT → SOURCE → STATE → CALCULUS → MODES → LAYERS → CAPABILITY → RUNTIME → OBSERVATION → ACTION → PROOF → ADMISSION`

R108 reuses the accepted authorities already present in OMEGA:

- R107 Drive/source corpus correlation and scale truth.
- R107 full-mode/calculus execution fabric.
- R104 eight functional layers: State, Intelligence, Memory, Relation, Computation, Action, Observation and Proof.
- R83 recovered 100-system / 36-menu-option / 18-capability ledger.
- R103 deterministic federation intent router.
- R85 workflow and R87 project continuity.
- R86 real-operation proof receipts.
- OMEGAv6 canonical admission authority.

## Minimum lawful capability set

All 18 recovered capabilities remain available. An operator goal activates only the capabilities required for the requested outcome and explicitly supporting capabilities inferred from the goal. AVAILABLE does not mean executed. SUPPORTING does not silently become REQUIRED.

This is deliberately different from turning every recovered mode, system, donor, source, solver or cloud node on simultaneously. Maximum capability means the correct authority is reachable when relevant.

## Workflow continuity

A new R85 workflow stores an additive R108 `capabilityPlan` beside its existing `modePlan`. The R85 schema stays stable so older persisted sessions remain readable. The stored plan records the starting goal/capability correlation; the visible membrane can recompute current mode and federation context from the current packet and current status observation.

Checkpoint receipts include the capability-plan snapshot so the operator can reconstruct not only what route was used but why that capability path was selected.

## Operator membrane

The R108 membrane is progressive disclosure in normal document flow. It shows:

- Drive/source authorities and whether each is ACTIVE, SUPPORTING or AVAILABLE.
- source-mode and canon/calculus composition, with gated inputs remaining non-executable.
- current eight-layer applicability.
- REQUIRED and SUPPORTING recovered capabilities and their target surfaces.
- R103 cloud/native handoff topology and current readiness only when live status was actually observed.
- empirical-validation requirements and proof gates.
- 20,736 resident vs 248,832 representational scale selection and 61,917,364,224 virtual address capacity.
- resulting operator routes.

It does not use fixed or absolute positioning and therefore does not cover a primary visual instrument.

## Federation boundary

R108 calls the R103 federation router; it does not duplicate its logic. Genesis remains PROPOSE, Optical SCREEN, Sovereign Compute SOLVE and OMEGAv6 ADMIT. A topology can be resolved without claiming current node readiness. Current readiness is exposed only after a federation status observation is supplied.

## Truth boundary

R108 never promotes catalog affinity into execution, a Drive design row into current runtime proof, an internal deterministic AutoPing into network activity, a simulation into measurement, a cloud registration into ONLINE state, or 20,736 / 248,832 / 61,917,364,224 into literal physical dimensionality. External scientific claims still require independent measured evidence and the accepted empirical-validation gates.
