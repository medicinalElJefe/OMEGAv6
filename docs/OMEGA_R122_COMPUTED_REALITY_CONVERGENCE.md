# OMEGA R122 — Computed Reality Convergence

## Purpose

R122 turns the newest whole-sphere Earth, Hybrid, federation, woven-continuity, 20,736-address, proof-ledger, and satellite-render work into one explicit construction boundary without deleting or replacing any working R114–R121 layer.

## Preserved working authorities

1. OMEGAv6 remains the canonical admit/prove/replay authority.
2. Genesis remains proposal/exploration authority.
3. Sovereign remains native execution authority only when authenticated heartbeat/device proof is present.
4. Optical/Living-Light remains the specialist optical design surface and may not be treated as the canonical runtime.
5. The R95/R119 20,736-cell membrane remains source-bound; R121 whole-sphere Earth is additive.
6. Existing route, archive, federation, machine-adapter, RCWA, navigation, proof, and UI layers are inherited rather than flattened.

## Computed-reality claim boundary

OMEGA may display computed proof surfaces today, but a full photoreal satellite-to-first-person frame may be called **computed** only when all of the following occur in one replayable execution:

`source packet -> authoritative time -> calibration -> orbit/attitude -> world rays -> atmospheric correction -> association -> depth/topology -> motion/trajectory -> lemma packet -> admission -> scar -> persistent world -> observer projection -> field/geometry compile -> physical shading -> temporal resolve -> frame proof -> encode`

Generated concept art is not a computed frame. A browser globe is not the completed renderer. A successful process exit is not physical validation.

## Woven continuity runtime law

The runtime carries the relational chain:

`Parent -> Expression -> Skin -> Constraint -> Integration -> Compression -> State -> Inheritance -> Burden -> New Parent`

Scar/history carry is explicit:

`Sigma(t+1) = rho_H * Sigma(t) + I_H(t)`

The renderer must therefore preserve history and provenance rather than visually resetting after a state change.

## Projection relativity

Equal projections do not prove equal internal states. An observer changes the admitted view, LOD, parallax, and resolution allocation; it does not silently delete canonical state.

## 20,736D interpretation

The 20,736 atlas is a complete address space for its declared `D × P × R × L` schema. It is not a claim of literal 20,736-dimensional physical spacetime. Host mappings must earn physical meaning through units, frames, calibration, and external validation.

## R122 locked invariants

- One authoritative state owner.
- Time before interpretation.
- Units and reference frames on physical quantities.
- Completion is separate from admission.
- Scar survives recovery.
- Lens weights are host-calibrated, never assumed universal.
- Deterministic replay is required for proof capture.
- No hidden generated-scene fallback.
- Every visible primitive in a claimed computed frame must have source lineage.
- PC ONLINE is never claimed without authenticated heartbeat/device proof.
- External surfaces are never claimed live without direct verification.
- Schema expansion is driven by persistent structured residual and held-out improvement.

## R122 public proof surface

`/r122-computed-reality.html`

This surface is deliberately additive. It reads canonical runtime status, Hybrid proof status, Earth catalog status, federation roles, the exact 21-stage numerical path, and the locked truth boundary. Its globe is an inspection visualization only; the surface explicitly refuses to call it the completed photoreal numerical renderer.

## Next executable closure

The next build must not add another decorative view. It should connect recorded or live satellite observation packets to a persistent world-state reconstruction and produce a first-person frame whose geometry and shading are traceable through the proof ledger. Benchmarks must compare that path against strong conventional navigation, photogrammetry/multiview, depth, motion, and rendering baselines before any superiority claim is admitted.
