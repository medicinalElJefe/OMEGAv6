# R257 · 20,736-coordinate Adaptive Experience Shell

R257 implements the uploaded 20,736-row UX build chart as a concrete presentation layer over the production-green R255 runtime. The chart is interpreted as an addressable 12×12×12×12 implementation space: 12 experience families × 12 build phases × 12 implementation planes × 12 build/verification steps = 20,736 coordinates.

## User-facing convergence

The operational shell exposes six first-class experience modes: Explore, Operate, Visualize, Analyze, Build, and Prove. Each mode changes presentation priority, promoted routes, workspace and lens defaults, never underlying evidence or authority. A separate Focus → Advanced → Full depth axis controls progressive disclosure. Immersive mode removes nonessential chrome while retaining the truth ribbon and a direct exit.

## Contextual navigation

Each experience promotes four already-registered R82 destinations and preserves `All systems` as an unconditional escape hatch. No route is deleted or semantically rewritten. R257 writes the established R82 workspace/lens preference keys so the new shell converges with the prior Home model rather than creating a parallel navigation universe.

## State and persistence

Only presentation state is persisted under `omega:r257:experience`: experience, depth and immersive preference. Legacy Home view preference keys are synchronized for compatibility. Evidence, proof, execution state, CanonState, Hybrid heartbeat state, source promotion and deployment state are never written by this layer.

## Truth contract

`PRESENTATION_MAY_TRANSFORM_TRUTH_CLASSIFICATION_MAY_NOT` is the R257 invariant. UNPROVEN cannot become PROVEN from view state; reconstruction cannot become observation; forecast cannot become observation; private-PC execution still requires current authenticated evidence. R125 admission, R141 returned-proof closure, R147 execution/dispatch, R240 source promotion and `ci.yml` production deployment remain unchanged.

## Responsive and performance behavior

The shell is sticky where operator context must remain visible, horizontal-scrolls mode/tool choices on narrow screens, preserves touch-safe controls, honors reduced motion, and uses progressive disclosure instead of destroying underlying capability. Focus hides low-priority technical boundary copy; Advanced restores context; Full restores complete technical boundary visibility.

## Promotion

R257 adds no workflow authority. Its focused invariants are imported through the existing R255 → R239 proof lane. Promotion remains exact-head PR proof, R210 collision/base fence, governed two-parent merge, canonical `ci.yml` deployment and live exact-SHA verification.
