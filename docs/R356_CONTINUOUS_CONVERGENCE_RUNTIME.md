# R356 — Continuous Convergence Runtime

R356 is a phase boundary, not another feature pile. It composes the already-proven OMEGA authorities into one fail-closed convergence kernel while preserving their ownership.

## Canonical loop

OBSERVE → NORMALIZE → RELATE → PARTITION → CARRY → CONSTRUCT → PRUNE → TEST → FALSIFY → PROVE → ADMIT → OBSERVE.

GitHub, Cloud, Sovereign PC, and external Observation remain distinct relative frames. A green source/build/deploy state is never silently promoted into another frame's truth.

## Woven Continuity

R356 operationalizes the established sequence: partition → exchange/transform → invariant carry → scar/history carry → re-contextualize/repartition → prove. Failed candidates are retained as scars. Atlas levels 12 → 144 → 1728 → 20,736 → 248,832 are address/representation resolutions only.

## Mandala containment

EXPLORE ⊃ CANDIDATE ⊃ BUILT ⊃ TESTED ⊃ ATTESTED ⊃ CANONICAL.

Freedom is highest outside; authority is highest inside. Candidate generation never grants production authority.

## Authority preservation

- Canon admission remains R125.
- Production writer remains .github/workflows/ci.yml.
- Existing R141 return proof, R146 history, R147 dispatch, R240 recursive self-build, R295 proof admission, R314 autonomous convergence, R315 Woven State Evolution, R353 lineage, and R355 temporal traversal remain inherited authorities.
- R356 adds composition and convergence; it does not bypass predecessor gates.

## Acceptance

A successor can be admitted only when exact parent/candidate identity is bound, all relative frames converge, rollback parent is retained, admission is authorized, and return proof exists. Direct production mutation is forbidden.


## Atlas 360 triangulation promotion

R356 now consumes the Dewey Atlas360 / 20,736-address triangulation canon as an additive derived proof layer. The source package is bound by SHA-256 for the exact 7,464,960-row L4 tensor, 678,240-row L1-L3 tensor, 22,620-address registry, formula canon and real-anchor evaluation schema.

The runtime does not ship or eagerly materialize the 7.46M-row tensor. Address/bearing geometry is deterministic, so OMEGA regenerates only the active 360-bearing slice and keeps a bounded LRU cache. This preserves startup/network/memory performance while representing the full 8,143,200 recursive address-bearing field, 73,288,800 triangle-class/anchor evaluation slots, and 219,866,400 slots with explicit sigma expansion.

Integration is monotonic:

- R349-compatible typed fields can be sampled at a selected Atlas360 bearing without mutating field state.
- opposite bearings are paired as theta ↔ (theta+180) mod 360 so frame-relative direction is separated from invariant state;
- GEOMETRIC / TEMPORAL / RELATIONAL triangle closure accepts only explicit independent anchors and explicit transforms;
- non-closure is returned as a residual and never forced to zero;
- scar carry is explicit and additive;
- missing real anchors/transforms yields HOLD, not synthetic closure, evidence or forecast data;
- R356 convergence exposes Atlas360 as advisory proof/relativity information only. It does not modify the existing admission result or STAY/TURN/ESCALATE decision;
- R125 CanonState admission, R141 return proof, R146 durable history, R147 dispatch and ci.yml production authority are unchanged.

The capability is visible in Convergence, Field/Data Motion and Evidence & Proof through one shared R356 surface derived from the current product authority rather than a new competing UI root.
