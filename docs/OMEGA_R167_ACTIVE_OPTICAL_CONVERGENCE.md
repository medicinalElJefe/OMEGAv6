# OMEGA R167 — Active Optical R153.2 Convergence

R167 removes a current-generation routing mismatch on top of the R166 mainline without flattening or rewriting the proven runtime.

## Active change

The canonical Cloudflare service binding `OMEGA_OPTICAL_MACHINE` now targets `omega-optical-machine-r1532`, the independently deployed and live-proven adaptive Optical SCREEN coprocessor. The current operator surface registry is aligned to the same generation.

R167 does **not** replace `src/workerR116.js`. The R116 Worker remains the canonical execution spine, with later organs mounted additively. All seven Durable Object bindings, Workers AI, assets, version metadata, Genesis surface transport, and Genesis machine transport remain present.

## Preserved lineage

R115 remains the initial machine-adapter evidence layer. R152 remains the operational Optical screen/UI and truthful Tier-2 preparation layer. R153.1 remains the external-tool push layer. R153.2 remains their additive adaptive-search successor. Historical source, tests, workflows and deployment configuration are not deleted merely because the active peer advances.

## Optical authority

R153.2 remains `SCREEN_ONLY`. It may rank atlas addresses, screen reduced-order structures, detect boundary pressure, run bounded deterministic adaptive search, and prepare a full-wave handoff. It may not convert those results into RCWA/FDTD/FEM proof, fabrication validation, measured dispersion, physical measurement, PC-online proof, returned full-wave proof, or CanonState admission.

A Tier-2 handoff remains `PREPARED_NOT_SOLVED` until a real full-wave solver returns a current authenticated result receipt through the Sovereign execution plane.

## Federation law

The intended operational chain remains:

`PROPOSE -> SCREEN -> SOLVE -> ADMIT`

The state law remains:

`reachable != writable != executed != verified != promoted`

OMEGAv6 remains the canonical control surface and R125 remains the sole CanonState admission authority. Genesis proposes. Optical screens. Sovereign/Hybrid may solve only when current authenticated transport and return proof exist.

## Vercel boundary

The protected Living Light target remains:

`https://omega-living-light-etching-private-woven2.vercel.app/`

R167 does not claim writable Vercel authority or same-URL promotion. `sameUrlPromotionAuthorized` remains false until independent Vercel project access proves otherwise. The machine execution path is therefore allowed to advance independently of the protected human-surface deployment.

## Atlas and calculus truth

`12 -> 144 -> 1728 -> 20,736 -> 248,832 -> 61,917,364,224` remains an address/composition-resolution hierarchy, not a claim of literal physical dimensionality. Orientation remains factored through `sigma in {-1,0,+1}`. The 37/73 pair remains a reference kernel/bias unless independently validated; it is not hard-coded as universal asymmetry/symmetry.

## Promotion gate

R167 is eligible for canonical promotion only when:

1. its focused invariants pass;
2. R153.2/R153.1/R152/R115 Optical lineage tests pass;
3. R166 and R164 truth-layer tests pass;
4. the full inherited production regression and Vite build pass;
5. canonical Wrangler dry-run passes;
6. the normal governed `ci.yml` production deploy succeeds after an exact two-parent merge;
7. live canonical health and federation proof show the promoted binding without inventing PC, solver, fabrication, measurement, or Vercel authority.
