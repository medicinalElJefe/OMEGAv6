# OMEGA R130 — Operational Control Plane

R130 turns the accumulated R121→R129 stack into one machine-readable operational organization layer while preserving every inherited truth boundary.

## Public runtime spine

`wrangler.jsonc` now points to `src/workerR130.js`. R130 wraps the proven R116 execution spine rather than replacing it, re-exports all Durable Object classes, and passes unknown routes through unchanged.

## Hierarchy

`1 seed → 12 organs → 144 branches → 1,728 cells → 20,736 lanes`

This remains logical execution/address resolution. It is not a claim of literal physical dimensionality or 1,728 independently deployed cloud accounts.

## Organized stack

- R121 — swarm execution fabric
- R123 — organism hierarchy
- R125 — autonomic routing + canonical admission authority
- R126 — causal candidate engine + maximum runtime orchestration
- R127 — evidence/execution/proof fabric
- R128 — external empirical calibration + deterministic replay identity
- R129 — reproducible experiment artifact runtime
- R130 — operational registry + health matrix

## New endpoints

### `GET /api/system/manifest`

Returns the canonical operational registry, hierarchy, authority levels, stage order, public entrypoint and truth boundaries.

### `GET /api/system/operational`

Probes inherited health/status/restoration/Hybrid/convergence/federation/autonomic endpoints through the inherited worker spine and reports one normalized matrix.

The matrix distinguishes:

- route reachability;
- canonical runtime state;
- authenticated PC heartbeat state;
- logical swarm hierarchy observation;
- missing/degraded routes;
- proof boundaries.

It explicitly does **not** reinterpret HTTP reachability as execution proof, an authenticated heartbeat as scientific validation, a logical 1,728-cell manifest as 1,728 independent cloud deployments, or any validation result as CanonState.

## Operational law

`source → provenance → causal compile → bounded plan → execution → receipt → verification → external holdout → replay ledger → R125 admission`

Every stage remains separately typed and separately auditable.

## Maximum-capacity principle

Maximum useful capacity is not maximum simultaneous mutation. It is maximum addressable capability under bounded cost, explicit provenance, deterministic replay, recovery, proof receipts, uncertainty carry, scar/history carry and narrow canonical authority.

R130 therefore makes the system easier to operate as it becomes larger: one public entrypoint, one machine-readable registry, one health matrix, preserved subsystem routes, and no reduction in inherited proof requirements.
