# OMEGA R113 — Full Convergence Pass

## Purpose

R113 is an additive convergence release over the verified R112 production line. It does not create a new application, new CanonState, new renderer authority, or new cloud authority. It closes a specific archive→runtime gap: the live continuous-field renderer still expressed continuity primarily through scalar CΩ / Φ / q / Λ / scar channels, while the later Woven Continuity canon had advanced to constrained directional redistribution with orientation, recoverable path, blocked carry and residue/history.

## Preserved authority stack

- One canonical state/proof admission authority in OMEGAv6.
- Genesis = PROPOSE.
- Optical = SCREEN and Tier-1 optical ranking/queue preparation.
- Sovereign PC = SOLVE when authenticated current device heartbeat exists.
- OMEGAv6 = ADMIT and durable proof/project continuity.
- Hybrid Link is transport/return-packet infrastructure, never a second state authority.
- Existing route-deferred specialist fabric, all current workstation destinations, mobile/desktop navigation, donor quarantine, evidence boundaries and solver queue lineage remain intact.

## Archive/runtime correspondence applied

The convergence review retains the established one-system module stack: persistent packet substrate, observer/Now frame, recursive scale compiler, traversal kernel, renderer field, evidence gate, proof ledger, dataset adapters, Hybrid bridge and packaging/repair surfaces. The current resident state is 20,736 addresses; 248,832 is representational expansion; 61,917,364,224 is a virtual/address-space scale. None is promoted to a literal physical dimension claim.

The already-established two-tier optical path is preserved rather than duplicated:

1. Tier 1: cheap scalar optical screen/rank over many candidates.
2. Tier 2: `OMEGA_FULLWAVE_QUEUE_v1` with RCWA preferred first and FDTD/FEM/GPU/measurement capacity remaining capability-gated.
3. Solver receipts return to OMEGAv6 as evidence candidates; only OMEGAv6 admits canonical transitions.

## R113 vector carry law

For each canonical route edge `i → j`, R113 derives a bounded carry projection:

- `Egress`: available continuity-bearing carry leaving the source.
- `Ingress`: transmitted carry that survives current constraint.
- `Blocked`: the non-transmitted partition of current egress.
- `Residue`: directional history derived from blocked carry plus inherited scar.
- `sigma ∈ {-1,0,+1}`: orientation separated from structural magnitude.
- `TURN`: reorientation pressure derived from contradiction/burden gradient, blocked carry and route acceleration.
- `Basin`: emergent convergence/recirculation detected from the current route graph, never a fixed decorative node.

The core balance is explicit and auditable:

`egress = ingress + blocked`

with reported numerical balance error.

Residue is history, not a hidden sink inserted into the balance equation. Recoverability remains a bounded function of plasticity, continuity, contradiction and residue.

## Symmetry / asymmetry correction retained

Symmetry means preserved/invariant structure under a declared transform. Asymmetry means direction, phase differentiation, path/history, local offset or emergence relative to a frame. 37/73 may remain an explicitly sourced reference kernel or bias parameter; they are not hard-coded as universal asymmetry/symmetry values.

## Visual implementation

`ContinuousFieldOverlayR13` now keeps the accepted domain bands and source-route flow while adding:

- transmitted vector-carry paths;
- blocked/residue markers;
- contradiction-gradient TURN vectors;
- emergent convergence/basin rings;
- the current admitted route emphasis;
- explicit instrument labels for INGRESS / EGRESS / BLOCKED / RESIDUE / TURN / BASIN.

This remains a deterministic computational projection. It is not an external physical measurement.

## Release gate

R113 must pass:

- constrained redistribution primitive checks;
- exact carry-balance expression;
- contradiction-gradient TURN derivation;
- route-topology basin derivation;
- signed orientation factoring;
- no shadow CanonState/local state write;
- inherited continuous-field invariants;
- inherited R112 Sovereign/Hybrid/federation invariants;
- full Vite production build;
- inherited OMEGA Cloud Bridge CI before production promotion.

## Next closed-loop target

After R113 is production-green, the highest-value remaining system proof is one complete federation run with current evidence at every handoff:

`operator intent → Genesis proposal → Optical scalar screen/rank → OMEGAv6 full-wave queue → Sovereign authenticated RCWA solve → result receipt → OMEGAv6 proof/admission → persistent project/ledger replay`

A structurally configured path is not the same as a completed live run. R113 keeps that distinction explicit.
