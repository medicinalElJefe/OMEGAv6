{
  "name": "omegav6-sovereign-runtime",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:routes": "node tests/route-cases.mjs",
    "check": "node --check src/worker.js && node --check public/app.js && npm run test:routes"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
