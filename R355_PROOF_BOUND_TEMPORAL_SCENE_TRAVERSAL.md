# R355 · Proof-Bound Temporal Scene Traversal

R355 advances R354 from one proof-bound scene receipt into a deterministic traversal surface across integer R350 model-time addresses.

## Composition

- Every node is an actual R354 scene receipt.
- Nodes are ordered by integer model tick.
- Each node link is SHA-256 over the previous link digest, tick, and R354 scene digest.
- One traversal requires stable R353 release-lineage identity across all scene nodes.
- HISTORY, NOW, and FORECAST remain explicit epistemic model relations.

## What this enables

A human can move across the established model-time field and retain exact provenance from release lineage → replayed field → packet/frame identity → derived render state → scene digest → temporal traversal link.

The chain therefore makes temporal visual navigation inspectable and reproducible without creating a second timeline or substituting rendered appearance for state identity.

## Authority boundaries

R355 creates no observed history, physical-time truth, CanonState admission, durable execution history, Hybrid exact-return authority, dispatch authority, or production authority.

- R125 remains sole CanonState admission authority.
- R141 remains exact Hybrid-return proof authority.
- R146 remains durable execution-history authority.
- R147 remains dispatch authority.
- `ci.yml` remains production promotion authority.
- 20,736 remains computational/address resolution, not a literal physical dimension.
- `NO_NEW_PHYSICAL_PRIMITIVE` remains enforced.
