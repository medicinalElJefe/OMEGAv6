name: SAR Public Workstation · GitHub Pages Fallback

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'GitHub Pages fallback is intentionally manual.'
          echo 'Repository-level Pages enablement is outside the GitHub Actions integration authority.'
          echo 'Primary standalone public hosting uses the isolated Cloudflare Pages release workflow.'
