import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const must=(ok,msg)=>{if(!ok)throw new Error('R88 '+msg)};
const nav=read('src/OmegaSideNavigatorR88.tsx');
const css=read('src/omegaSideNavigatorR88.css');
const shell=read('src/InstrumentOSShellR62.tsx');
const home=read('src/OmegaHomeR71.tsx');
const polish=read('src/responsivePolishR88.css');
const registry=read('src/omegaExperienceRegistryR82.ts');
const routes=[...registry.matchAll(/routes:\[(.*?)\]/gs)].flatMap(m=>[...m[1].matchAll(/'([^']+)'/g)].map(x=>x[1]));
must(routes.length===44&&new Set(routes).size===44,'canonical route universe must remain 44/44 unique');
must(nav.includes('OMEGA_ALL_ROUTES_R82')&&nav.includes('r88-route-scroll'),'global navigator must render the shared 44-route authority through one scroll owner');
must(nav.includes("layer==='SOFTWARE'")&&nav.includes('OmegaSystemInventoryR83'),'software inventory must remain directly reachable from the same side navigator');
must(nav.includes("omega-r88-open-navigator"),'navigator must expose one event entry point for Home and workstation');
must(css.includes('.r88-navigator{position:absolute;inset:0 auto 0 0')&&css.includes('overflow:auto'),'navigator must be a side pop-out with internal scrolling');
must(css.includes("@media(max-width:900px)")&&css.includes("width:min(92vw,390px)"),'mobile navigator must remain side-owned instead of becoming a buried page list');
must(shell.includes('OmegaSideNavigatorR88')&&!shell.includes("className='r62-rail'"),'workstation must use R88 global side navigator and retire the permanent/bottom R62 route rail');
must(home.includes('OmegaSideNavigatorR88')&&home.includes("omega-r88-open-navigator"),'Home must open the exact same global navigator');
must(polish.includes("html[data-omega-frame='desktop'] .omega-workstation-v2{padding-left:0!important}"),'desktop application viewport must recover space formerly reserved for permanent rail');
must(polish.includes('grid-template-columns:repeat(auto-fit,minmax(min(230px,100%),1fr))'),'responsive polish must reflow multi-card specialist grids without flattening desktop');
console.log('R88 GLOBAL SIDE NAVIGATOR + RESPONSIVE POLISH PASS · one side banner · 44 routes · mobile/desktop viewport authority');
