import assert from 'node:assert/strict';
import {validateSourceSpatialCalibrationR231} from '../src/world/sourceSpatialCalibrationValidationR231.js';
const stable=v=>Array.isArray(v)?v.map(stable):(v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v);
const sha256=async v=>{const bytes=new TextEncoder().encode(JSON.stringify(stable(v)));const hash=await crypto.subtle.digest('SHA-256',bytes);return [...new Uint8Array(hash)].map(x=>x.toString(16).padStart(2,'0')).join('')};
const h=c=>String(c).repeat(64).slice(0,64);
const camera={sourceIdentity:'camera-source',imageWidthPx:1920,imageHeightPx:1080,intrinsics:{fx:1200,fy:1200,cx:960,cy:540},pose:{referenceFrame:'WGS84-ENU:R218',position:{x:0,y:0,z:0},orientation:{x:0,y:0,z:0,w:1}}};
const cameraEvidenceSha256=await sha256(camera);
const reconstruction={state:'SOURCE_SPATIAL_RECONSTRUCTION_COMPUTED',geometrySha256:h('a'),receiptSha256:h('b'),cameraEvidenceSha256,referenceFrame:'WGS84-ENU:R218',r227BundleSha256:h('c')};
const frame={state:'SOURCE_SPATIAL_COMPUTED_FRAME_RENDERED',frameSha256:h('d'),receiptSha256:h('e'),r228GeometrySha256:reconstruction.geometrySha256,r228ReceiptSha256:reconstruction.receiptSha256};
const validationEvidence={sourceIdentity:'independent-survey',referenceFrame:'WGS84-ENU:R218',capturedOrAuthoritativeTime:'2026-09-08T19:00:00Z',provenance:'independent surveyed controls',acquisitionMethod:'SURVEY_CONTROL',uncertainty:{pixel:0.25,worldM:0.01},correspondences:[{u:960,v:540,world:{x:0,y:0,z:10}},{u:1080,v:540,world:{x:1,y:0,z:10}},{u:960,v:660,world:{x:0,y:1,z:10}},{u:840,v:540,world:{x:-1,y:0,z:10}},{u:960,v:420,world:{x:0,y:-1,z:10}}]};
const pass=await validateSourceSpatialCalibrationR231({frame,reconstruction,camera,validationEvidence});
assert.equal(pass.state,'INDEPENDENT_SPATIAL_CALIBRATION_VALIDATION_PASSED');
assert.equal(pass.independentCorrespondenceValidationPassed,true);assert.equal(pass.rmsResidualPx,0);assert.equal(pass.maxResidualPx,0);assert.equal(pass.correspondenceCount,5);
assert.match(pass.validationEvidenceSha256,/^[a-f0-9]{64}$/);assert.match(pass.receiptSha256,/^[a-f0-9]{64}$/);
assert.equal(pass.sourceAuthenticationProved,false);assert.equal(pass.spatialCalibrationProved,false);assert.equal(pass.globalPhysicalRegistrationProved,false);assert.equal(pass.computedPhotorealRealityProved,false);assert.equal(pass.solverValidityProved,false);assert.equal(pass.nativeExecutionClaimed,false);assert.equal(pass.pcOnlineClaimed,false);assert.equal(pass.federationClosureProved,false);assert.equal(pass.canonicalMutation,false);
const again=await validateSourceSpatialCalibrationR231({frame,reconstruction,camera,validationEvidence});assert.equal(again.receiptSha256,pass.receiptSha256);
const shifted={...validationEvidence,correspondences:validationEvidence.correspondences.map((c,i)=>i===0?{...c,u:c.u+20}:c)};const fail=await validateSourceSpatialCalibrationR231({frame,reconstruction,camera,validationEvidence:shifted,rmsThresholdPx:2,maxThresholdPx:5});assert.equal(fail.state,'INDEPENDENT_SPATIAL_CALIBRATION_VALIDATION_FAILED');assert.equal(fail.independentCorrespondenceValidationPassed,false);assert.ok(fail.maxResidualPx>=20);
const selfDerived={...validationEvidence,sourceIdentity:'camera-source'};const held=await validateSourceSpatialCalibrationR231({frame,reconstruction,camera,validationEvidence:selfDerived});assert.equal(held.state,'HELD_FOR_INDEPENDENT_CALIBRATION_EVIDENCE');assert.ok(held.missing.includes('validationEvidence.independence'));
const brokenFrame={...frame,r228GeometrySha256:h('f')};const lineageHeld=await validateSourceSpatialCalibrationR231({frame:brokenFrame,reconstruction,camera,validationEvidence});assert.equal(lineageHeld.state,'HELD_FOR_INDEPENDENT_CALIBRATION_EVIDENCE');assert.ok(lineageHeld.missing.includes('R230_R228_LINEAGE'));
console.log(`R231 INDEPENDENT CALIBRATION VALIDATION PASS · exact residual ${pass.rmsResidualPx}px · shifted control correctly FAIL ${fail.maxResidualPx}px · self-derived controls and broken lineage fail closed · global/photoreal/solver/PC/federation/Canon claims remain false`);
