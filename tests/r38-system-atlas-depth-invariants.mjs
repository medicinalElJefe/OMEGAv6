import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const must=(ok,msg)=>{if(!ok)throw new Error(msg)};
const atlas=read('src/SystemAtlasControl.tsx'),css=read('src/systemAtlasDepthR38_4.css'),pkg=read('package.json');
must(atlas.includes("import {CanonicalPacketTruthPlotR93} from './TruthVisualsR93'"),'System Atlas must bind the active packet to the direct canonical truth plot');
must(atlas.includes('<CanonicalPacketTruthPlotR93 record={record}')&&atlas.includes("atlas-r38-stage"),'System Atlas must lead with direct packet truth rather than procedural field geometry');
for(const token of ['current reality ≠ historical registration','V24 donor/debt labels remain visible only as predecessor evidence','No fake OPEN button','current successor state, and current successor route separate','evidence/device/provider-gated families remain visible and truth-gated'])must(atlas.includes(token),`System Atlas truth boundary missing ${token}`);
must(atlas.includes("const currentExecutable=new Set(['WEB_ACTIVE','SOURCE_ACTIVE','LOCAL_ACTIVE'])"),'current executable successor whitelist missing');
must(atlas.includes("canOpen=Boolean(current&&currentExecutable.has(current.successor)&&currentRoute!=='System Atlas')"),'Open eligibility must require an executable successor and non-self current route');
must(atlas.includes('V24 predecessor reality')&&atlas.includes('V24 predecessor target')&&atlas.includes('Current successor reality')&&atlas.includes('Current operator route'),'historical registration and current successor execution/route must remain separately inspectable');
for(const token of ['continuity','plasticity','contradiction','burden'])must(atlas.includes(token),`active packet context missing ${token}`);
must(css.includes('.atlas-r38-stage')&&css.includes('.atlas-r38-field')&&css.includes('@media(max-width:640px)'),'System Atlas field-first responsive containment missing');
must(pkg.includes('r38-system-atlas-depth-invariants.mjs'),'R38.4 System Atlas regression must be wired into full QA');
must(!atlas.includes('@appdeploy/client')&&!css.includes('appdeploy.ai'),'R38.4 must remain AppDeploy-free');
console.log('R38.4/R168 SYSTEM ATLAS DEPTH PASS · direct packet truth first · predecessor registration/current successor execution/current route separated · no fake Open · responsive containment retained');
