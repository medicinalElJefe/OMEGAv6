import fs from'node:fs';import assert from'node:assert/strict';const s=fs.readFileSync('src/sarFusionR280.ts','utf8');const must=(ok,msg)=>assert.ok(ok,'R280 fusion '+msg);for(const t of ['planSarFusionR280','REGISTRATION_REQUIRED','PRODUCT_LEVELS_DIFFER','MODEL_STATE_PRESENT','UNBOUND_SOURCE_PRESENT','MEASUREMENT_SURFACES_DIFFER','provenance-preserving-fusion','fusionTruthClassR280'])must(s.includes(t),'missing '+t);must(s.includes('cannot claim direct-observation truth'),'fusion must remain derived');console.log('R280 fusion invariants PASS');