import fs from 'node:fs';
const must=(ok,msg)=>{if(!ok)throw new Error(msg)};
const home=fs.readFileSync('src/OmegaHome.tsx','utf8');
const field=fs.readFileSync('src/OmegaLivingField.tsx','utf8');
const baseCss=fs.readFileSync('src/omegaLivingField.css','utf8');
const sovereignCss=fs.readFileSync('src/omegaSovereignVisualR10.css','utf8');
const app=fs.readFileSync('src/App.tsx','utf8');
must(home.includes("import OmegaLivingField from './OmegaLivingField'"),'home must mount the restored animated visual');
must(home.includes('<OmegaLivingField address={address} onSelectAddress={setAddress}/>'),'startup visual must bind and mutate the real canonical phase address');
for(const x of ['corpusState','decodeAddress','unifiedFromRecord','20,736-state packet','phase · scar · continuity · burden · proof · admitted route'])must(field.includes(x),`living field source/truth contract missing ${x}`);
for(const x of ['OBSERVER','SHELL','CLOSURE','ROUTING','PROOF','PROJECTION'])must(field.includes(`'${x}'`),`launch sequence missing ${x}`);
for(const x of ['continuity / carry','contradiction / scar pressure','proof / admitted route'])must(field.includes(x),`visual legend missing ${x}`);
for(const x of ['quadraticCurveTo','bezierCurveTo','phaseStates','routeStrength','wakeCount'])must(field.includes(x),`deep relational visual grammar missing ${x}`);
must(field.includes('requestAnimationFrame'),'visual field must actually animate');
must(field.includes('prefers-reduced-motion'),'visual field must respect reduced-motion preference');
must(baseCss.includes('.olf-stage canvas')&&sovereignCss.includes('@media(max-width:720px)'),'visual field must have bounded desktop/mobile canvas presentation');
must(app.includes("import './omegaSovereignVisualR10.css'"),'unified visual hierarchy must be activated last');
must(sovereignCss.includes('.omega-nexus-card')&&sovereignCss.includes('.r4-welcome')&&sovereignCss.includes('.workstation-main'),'home, menus and workstation must share the unified visual layer');
must(!field.match(/Math\.random|generated satellite|synthetic street/i),'launch visual must not invent external evidence');
console.log('LIVING_VISUAL_R10 PASS · deep source-bound field + unified home/menu/workstation visual hierarchy');
