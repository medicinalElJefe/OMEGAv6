import fs from'node:fs';import assert from'node:assert/strict';const s=fs.readFileSync('src/sarForecastTruthR280.ts','utf8');for(const t of ['OBSERVED','CORRECTED','FUSED','ASSIMILATED','SIMULATED','FORECAST','earthStateFromObservationR280'])assert.ok(s.includes(t),'R280 Earth truth missing '+t);assert.ok(s.includes('never inherits direct-observation truth'),'model/forecast state must not become observation truth');console.log('R280 Earth truth invariants PASS');