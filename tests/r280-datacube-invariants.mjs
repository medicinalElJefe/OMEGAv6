import fs from'node:fs';import assert from'node:assert/strict';const s=fs.readFileSync('src/sarDataCubeR280.ts','utf8');const must=(ok,msg)=>assert.ok(ok,'R280 cube '+msg);for(const t of ['cubeAddressR280','cubeTileR280','cubeCompatibilityR280','CRS_MISMATCH','MEASUREMENT_SURFACE_MISMATCH','PRODUCT_LEVEL_DIFFERS','TRUTH_CLASS_DIFFERS','NATIVE_RESOLUTION_DIFFERS','lazy:true'])must(s.includes(t),'missing '+t);must(s.includes('never overwrites native geometry'),'must preserve native-source truth');console.log('R280 data cube invariants PASS');