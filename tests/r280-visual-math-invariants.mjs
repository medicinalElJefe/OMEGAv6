import fs from'node:fs';import assert from'node:assert/strict';const s=fs.readFileSync('src/sarVisualMathR280.ts','utf8');for(const t of ['PHASE_CYCLIC','SIGNED_DEFORMATION','normalizeDbR280','normalizePhaseR280','normalizeSignedR280','displayScaleBoundaryR280'])assert.ok(s.includes(t),'R280 visual math missing '+t);assert.ok(s.includes('display transform only'),'display normalization must remain non-authoritative');console.log('R280 visual math invariants PASS');