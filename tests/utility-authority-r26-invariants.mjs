import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const legacy=read('src/OmegaSpecialistSuite.tsx');
const authority=read('src/OmegaUtilityAuthorityR26.tsx');
const workstation=read('src/OmegaWorkstationFullV2.tsx');
assert.equal(legacy.trim(),"export {default} from './OmegaUtilityAuthorityR26';",'legacy specialist suite must be a thin compatibility alias only');
for(const route of ['Field','Data Motion','Convergence','Projects','Render Queue','Assets','Evidence & Proof','Memory','Canon Evolution','Governance','Instructions','Settings','System'])assert.match(authority,new RegExp(route.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')),`R26 utility authority missing ${route}`);
assert.match(authority,/SOURCE-DERIVED INSPECTOR/);
assert.match(authority,/BROWSER-LOCAL/);
assert.match(authority,/Current Drive release pointer.*false/s);
assert.match(authority,/UNRESOLVED IN HOSTED RUNTIME/);
assert.match(authority,/Native GPU image\/video production remains unavailable/);
assert.match(authority,/not model training, Drive memory, or cross-device persistence/);
assert.doesNotMatch(authority,/evaluateCorpusModes/);
assert.doesNotMatch(authority,/179 modes present/);
assert.doesNotMatch(authority,/Earth truth gate',true/);
assert.doesNotMatch(authority,/Drive pointer remains B015 R1/);
assert.match(workstation,/OmegaSpecialistSuite/);
console.log('R26 UTILITY AUTHORITY PASS · one active utility implementation · browser/source/evidence boundaries explicit');
