# OMEGA Restore / Surpass Addendum — R5 Full Interaction + Inheritance Repair

## RELEASE / LINEAGE
- Required first read completed against Google Drive `.OMEGA_HYBRID_HUB/00_CONTROL/LATEST_OMEGA_UPDATE.json` (file id `1KKJKXeQf-IEZdej0BhiQrhykv2JLv9Nw`).
- Local package lineage remains `OMEGA SELF MONITORING DELTA MESH HUB B015` / R1 with SHA-256 `8be50fbe717679d8044714d1c4541f94061ac0c8d5f5d9201d76d8cb7421e379`, immutable-core preflight, B007-B015 gates, crash watchdog, changed-file rollback, and activation evidence.
- Drive pointer scope still says `local OMEGA only`; the separate owner public-promotion authorization dated 2026-08-29 continues to permit exact verified public restore-and-surpass candidates without waiving any test, truth, rollback, portability, or canonical-runtime verification law.
- Parent public `main` for R5: `4ea908adfabd252bf71907fa0185aaad2eed5e7e` (R4 promoted build).
- Active integration branch: `surpass-mainline`, confirmed equal to current main before R5 edits.
- R4 main workflow `33256617255` previously passed verify, Cloudflare deployment, canonical public runtime verification, and deployment receipt. User browser evidence after that deployment nevertheless establishes remaining UX/function regressions; CI success is therefore not treated as proof that every interactive control works.

## HOURLY + 3-HOUR INHERITANCE RECONCILIATION
The enabled `OMEGA Sovereign Hourly Build` and `OMEGA 3-Hour QA Repair` tasks were explicitly inspected in this repair turn. Their accumulated requirements are adopted as R5 acceptance law rather than treated as background prose:
- first-read Drive authority and exact lineage reconstruction;
- current-main-based `surpass-mainline` only;
- full cross-chat / cross-build donor reconciliation;
- B058/V90 useful-source restoration, not label-only replacement;
- 179-mode execution and Mode188 retention;
- B020 route-before-generation;
- sovereign Cloudflare/platform adapters and no AppDeploy dependency;
- canonical Worker verification after promotion;
- rollback on promoted-runtime failure;
- no dead menu/tool/command may be counted as restored.

## LIBRARY / HISTORICAL SOFTWARE EVIDENCE RE-READ
Fresh ChatGPT Library search was performed rather than relying on one recent donor.

### `OMEGA_ALL_SOFTWARE_61917364224D_FULL_BUILD_v22.xlsx`
Evidence establishes the intended active families and roles: Atlas Desktop/runtime OS, Reality Compiler, Persistent Packet Substrate, Hybrid Link, CanonForge, VGCL, Executable Atlas Generator, Shell/Mandala, Field Render Engine, Earth Traversal, Biology Traversal, Patch/Recovery and related families. Rendering requirements explicitly reject UI-only fake state, decorative dots, scene-swap substitutes, infographic-only renderers and fake terrain presented as fact.

### `OMEGA_UNIVERSAL_INTRINSIC_RENDER_MACHINE_FULL_LEDGER.md`
The target is explicitly not a dashboard or sci-fi shell. The intended product is one universal intrinsic render machine with one launcher, one runtime, one state authority, one renderer path, one command registry, one project database, one evidence ledger, one capability registry and one update authority. Acceptance requires every menu/tool/command either to work or to be honestly disabled; ornamental controls are not acceptable.

### `all_excel_files_unified_relativity_ledger.xlsx`
Retains Earth topology, motion relativity, exact 20,736 lattice, seed-chain/phase-elasticity and corpus honesty boundaries. Repeated workbooks are views, not independent confirmation.

### `OMEGA_Master_Ledger_Milestone03.xlsx` and B009 ledgers
Retain runtime bootstrap, authority/capability registries, scheduler, scene graph, traversal, render graph, workspace, plugin host, validation, installer, canonical StatePacket, checkpoint/replay and native/GPU boundaries. Hosted cloud restoration must not falsely relabel unbound native/GPU/production backends as live.

### Historical instruction corpus
Prior cross-chat instruction explicitly requires all modes/calculus/software knowledge, all build attempts, archived data, full menu correlation and a single canonical StatePacket/transition path. R5 adopts that as a regression-by-omission target while still requiring source evidence for each migrated implementation.

## FIRST-HAND INTERACTION DEFECTS FOUND IN R4
R5 identified concrete implementation defects that explain the user's report that buttons do not work:
1. `OmegaLauncher.tsx` used an outer `<button>` for every launcher card while nesting the favorite `<button>` inside it. Nested interactive buttons are invalid HTML and can produce unpredictable click/focus behavior.
2. The active Modes registry rendered every mode result as a visual `<button>` with no `onClick`, so mode cards were genuinely dead controls.
3. Mode policy controls changed policy labels but did not provide a meaningful corresponding inspection view, making the interface appear nonfunctional even though all-mode computation existed.

## R5 SOFTWARE REPAIRS
- Launcher cards are now valid focusable `role="button"` controls with click plus Enter/Space activation. Favorites remain independent real buttons with propagation containment. All 44 route names are retained.
- Mode result controls now execute a real inspection action, retain selected-mode state, and expose selected mode category/gate/score.
- Mode view controls now provide actual `ALL`, top-24 contextual view, and selected-mode views. These are presentation/inspection filters only; **ALL registered modes continue to execute against the canonical packet**.
- A new interaction-contract invariant suite checks 44 launcher/workstation route parity, prevents the nested-button regression, proves launcher keyboard/click activation, proves mode-card action binding, proves Home/Command/Hybrid action wiring, and reasserts route-before-generation.
- The interaction suite is part of `test:navigation`, therefore part of the complete static verification and production build gate.

## STILL MISSING / NOT TO BE FALSELY CLAIMED
R5 is a full-restore development continuation, not a declaration that every historical capability is already live. Remaining bounded work includes:
- exact donor-by-donor button/action audit across every specialist surface, including controls inside legacy/specialist components;
- exact useful SVG migration and runtime binding beyond currently adapted vector geometry;
- exact Earth workbook/GIS/DEM/LiDAR/OSM donor ingestion and evidence adapters beyond bundled geographic geometry;
- deeper Matter shader/palette calibration against declared model variables and physically truthful legends;
- Relativity motion manifestation beyond static readouts, using phase/carry/observer relationships as executable visual behavior;
- full universal `render_anything` output closure (image/video/audio/proof) where supported by a real backend;
- native Windows/desktop/GPU execution, pairing and proof; cloud/browser code may not impersonate it;
- Hybrid native device actions until authenticated device proof exists;
- complete installer/one-click native package parity and hardware validation;
- production media backends not actually bound in the hosted Worker.

## R5 ACCEPTANCE RULE
No route is considered restored merely because a label exists. A route must resolve to an implementation, and each control presented as interactive must change state, navigate, execute a bounded operation, or be honestly disabled with its unmet requirement. Full restore remains evidence-based and incremental until the entire historical capability ledger is either migrated, adapted, explicitly superseded, or truthfully quarantined.
