# OMEGAv6 Restore + Surpass Manifest

Status: **PROMOTED SOURCE RESTORE — PUBLIC RUNTIME TARGET LOCKED; END-TO-END FULL RESTORE NOT CLAIMED WITHOUT DIRECT VERIFICATION**. The cloud-hosted sovereign workstation source slice is promoted through the restore-and-surpass lineage with the canonical public target `https://omegav6.jeffdeweyeljefe.workers.dev/`. Google Drive remains canonical release authority through `LATEST_OMEGA_UPDATE.json` -> B015 R1.

The public Worker is designed to verify its own deployment context against the canonical hostname. `/api/health`, `/api/status`, and `/api/restoration` may report `LIVE_PASS` only when the request is actually served from the canonical workers.dev host and the runtime checks pass. Source/build success alone is not LIVE PASS and is not complete hosted inheritance. Hybrid Link native execution remains device-proof gated, Earth external feeds remain degraded until bound, and external synthesis remains provider-bound.

## COMPLETION GATE
The source restoration gate requires React/Vite build PASS; Worker syntax/Wrangler dry-run PASS; fixed 30-case route regression PASS; restore invariants PASS; source-pack/unified-calculus validation PASS; responsive containment; no false provider/native/live-feed claims; all registered routes mapped to specialized source instruments, durable local operations, or explicit truth-gated integration surfaces; and direct public-host verification on the canonical workers.dev deployment. Menu count never substitutes for functional inheritance evidence.

## MIGRATED_EXACT
- `src/main.tsx`
- `src/index.css`
- `src/ResponsiveRuntimeShell.tsx`
- `src/responsiveShell.css`
- `src/hybridCommandRuntime.ts`
- `src/OmegaCommandDeck.tsx`
- `src/commandDeck.css`
- `src/PanelBoundary.tsx`
- `src/corpusEmbedded.ts`
- `src/corpusPack.ts`
- `src/corpusRuntime.ts`
- `src/sparseChunk1.ts` through `src/sparseChunk5.ts`
- `src/unifiedCalculus.ts`
- `src/AtlasViewport.tsx`

## MIGRATED_ADAPTED / NEW SOVEREIGN SUPERSET
- `src/App.tsx` — restored + surpass workstation entry plus tested Woven Continuity Build Out control plane.
- `src/OmegaSurpassShell.tsx` / `src/OmegaWorkstation.tsx` — sovereign workstation using the embedded 20,736-state corpus, source routing, 179-mode evaluator, unified calculus, Atlas, traversal, assistant-first Command Center, local proof/memory/projects/jobs persistence, validation, Hybrid Link truth gate, Earth truth gate, Relativity+, 20,736 Field+, Atlas Calc+, Infinity+, Scale+, Reality Lab+, Forecast+ and the multi-intelligence fabric.
- `src/intelligenceFabric.ts` — **NEW_SURPASS / DONOR-ORCHESTRATION** registry for historical SAI, bounded planner/macro, local training-pipeline, RAG/planner, verified assistant/automation donor runtime, render-conditioner, Forecast+ and Mode188/ALL MODES intelligence contributors. Contributors are classified by role and truth state; donor architecture or training pipelines are never mislabeled as loaded neural weights.
- `src/IntelligenceFabricPanel.tsx` / `src/intelligenceFabric.css` — **NEW_SURPASS** responsive multi-intelligence orchestration and provenance surface for CHAT, CODE, FORECAST, TRANSLATE, RENDER, PROOF, HYBRID and MEMORY intents.
- `src/lensCalculus.ts` / `src/lensCalculus.css` — **NEW_SURPASS** distinct declared internal calculus for UNIFIED, WATER, LIGHT, SCAR, RELATIVITY, FORECAST, PROOF and TOPOLOGY visual lenses. Each lens exposes equation/family/meaning/truth boundary in the UI instead of acting as a color-only filter.
- `src/menuUniverse.css` plus `src/ResponsiveRuntimeShell.tsx` adaptation — **NEW_SURPASS** searchable 44-surface workstation universe on desktop and grouped touch-first MENU mode on mobile, while retaining the 24 software-family registry and explicit AUTO/DESKTOP/MOBILE layouts.
- `src/forecastRuntime.ts` — **NEW_SURPASS / MIGRATED_ADAPTED** multi-corridor source-state forecasting over ALL MODES, FUTURE, RELATIVITY, PROOF, WATER, FULL CANON plus one counterfactual first-turn corridor; it does not consume future observations.
- `src/ForecastSovereignPanel.tsx` — **NEW_SURPASS** human-readable competing-futures instrument with source-only prior freezing and later-state scoring.
- `src/CalculusTraversal.tsx` — donor-based source/calculus renderer adapted for cloud runtime.
- `src/PromptOrchestrator.tsx` — donor conversation/enact surface adapted to Cloudflare transport and provider/device gates.
- `src/platformAdapter.ts` — bounded Cloudflare HTTP + local continuity adapter.
- `src/cloudClientShim.ts` — donor import compatibility without false legacy-provider claims.
- `src/runtimeIdentity.ts` — B058/V90 donor lineage with separated Drive/Cloudflare/native authority.
- `src/workstation.css` — responsive desktop/mobile workstation layer.
- `src/worker.js` — B020 route-before-generation, canonical public-host verification, health/status/restoration, Hybrid Link/device gates, model-provider gate, and public runtime truth state.
- `package.json`, `vite.config.ts`, `wrangler.jsonc`, `.github/workflows/ci.yml`, fixed regression suites.

## INTELLIGENCE INHERITANCE TRUTH STATES
OMEGA intentionally does not collapse all historical AI work into a single fictional model. Current source distinguishes active hosted deterministic/router intelligence, verified donor runtimes, donor RAG/planner architecture, local training-pipeline donors, proof-bound SAI package semantics, provider-bound synthesis, and model-weight-required render backends. Historical SAI corpus/training code can be preserved and routed without claiming that compatible learned weights are present in the public Worker. Where a donor explicitly recorded missing model weights, that negative evidence remains binding until a real provider/local model is attached and verified.

## LENS CALCULUS CONTRACT
All lenses consume the same canonical source packet, but their visual score is not interchangeable. WATER emphasizes conductance under continuity/plasticity and resistance; LIGHT emphasizes evidence-supported coherent geometry; SCAR emphasizes persistent strain under contradiction/burden and curvature; RELATIVITY uses motion drive, route velocity and acceleration; FORECAST uses future plasticity, model forecast/evidence/gate and route direction; PROOF applies triangulation/admissibility factors; TOPOLOGY emphasizes relational-shell structure; UNIFIED uses the common coherence kernel. These are governed internal computational models and must not be represented as experimentally established physical laws.

## FUNCTIONAL ROUTE COVERAGE
Registered workstation routes resolve to executable source/calculus instruments, durable browser-local operations, or explicit external/device truth gates. Route or menu-label presence alone is not counted as complete restoration. The 44-surface menu universe is now searchable and device-adaptive, but each surface still must independently satisfy the route/control/execution/state-or-output/proof/failure-path acceptance rule before full functional inheritance is claimed.

## EXTERNAL / DEVICE BOUNDARIES
- Native PC execution / remote-screen control: `DEVICE_PROOF_REQUIRED` until a verified heartbeat/proof return exists.
- External synthesis provider: bounded failure when no provider binding exists.
- Local/neural model weights: `MODEL_WEIGHTS_REQUIRED` or proof-bound unless an actually compatible model is attached and verified.
- Earth live external feeds: `EXTERNAL_DEGRADED` until a verified source binding exists.
- Cross-client shared realtime transport: not represented as live until a Cloudflare-native shared transport is bound.

## PRESERVED ACCEPTED BEHAVIOR
- Google Drive pointer remains release authority.
- GitHub -> Cloudflare public bridge remains.
- `/api/health`, `/api/status`, `/api/restoration` remain public-safe and truth-gated.
- B020 route-before-generation remains.
- Explicit ALL MODES routes full; deterministic facts do not invoke synthesis.
- Provider absence returns bounded failure, never fabricated synthesis.
- Hybrid Link native execution remains `DEVICE_PROOF_REQUIRED` without heartbeat/proof.
- Earth external feeds remain degraded until verified.
- Local draft continuity, single-flight interaction discipline, mobile/desktop overflow containment, workers.dev route activation, rollback, and auth-continuity boundaries remain.
- 12/144/1728/20,736 and larger software address spaces are representational resolutions/addresses, not physical dimensions.

## PROMOTION RULE
Build restore-and-surpass deltas only on a branch created from current `main`. Promote to `main` only after the exact head passes CI/build/regression gates and the PR is conflict-free. Preserve the prior `main` SHA as rollback evidence. A stale surpass branch must be treated as historical donor lineage, not as a promotion branch. The public runtime URL remains `https://omegav6.jeffdeweyeljefe.workers.dev/` unless a separately verified public target supersedes it; AppDeploy is not part of this runtime contract.