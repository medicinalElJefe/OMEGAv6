# OMEGA Restore Manifest Addendum — SAI B059 R1

## Donor authority
Exact donor archive inspected from connected Drive: `SAI/OMEGA_SAI_B059_PART_01_CORE_COMPILED.zip` (Drive file id `1E7vk-RPG7NHsjaLMhKaCGMFmZPUIdtN-`). The donor contains the B059 standard-library Python runtime, compiled SQLite corpus, canon kernel, corpus compiler, source-grounded query engine, SHA-256 scar ledger, HTTP cockpit, tests and acceptance report.

The donor is **not** release authority. Drive `.OMEGA_HYBRID_HUB/00_CONTROL/LATEST_OMEGA_UPDATE.json` remains release authority and currently identifies OMEGA B015 R1. This migration does not mutate constitutional core or release pointers.

## Exact semantics restored/adapted
- 15 required corpus authorities with exact SHA-256 identities from the B059 build manifest.
- B059 donor compiled database identity: `compiled/omega_sai.db`, 747,786,240 bytes.
- Deterministic grounded retrieval plan with source-role provenance.
- Canon common-kernel channels CΩ, Φ, q, Λ and STAY / TURN / ESCALATE gate.
- Canon workflow: Sense → Normalize → Score → Gate → Act → Ledger.
- 188-step retained traversal contract, adapted to the current hosted canonical 20,736 admitted-next graph rather than impersonating the unloaded donor SQLite graph.
- SHA-256 previous-hash / entry-hash scar ledger persisted in browser storage.
- Donor self-test contract for database integrity, all sources, document floor, 82,082 donor edges, grounded query and ledger chain.
- Cross-build intelligence contributor fabric retained as a separately classified view.

## Surpass delta
The prior hosted SAI Lab mainly exposed a contributor/role registry. R1 adds executable hosted semantics: retrieval-plan compilation, exact corpus authority census, live current-state kernel scoring, 188-step source traversal inspection, browser SHA-256 scar ledger, donor-vs-hosted self-test separation and a deterministic hosted receipt.

## Truth boundary
The B059 README explicitly defines "trained" as corpus compilation/indexing/calibration and deterministic governed inference. It does not claim a scientifically demonstrated superintelligence or newly trained foundation model. The hosted Worker therefore records `compiledDonorDatabaseLoaded=false` and `foundationModelLoaded=false`; the 747 MB donor database remains a local/native donor until an explicit portable host binding is implemented and verified.

## Regression lock
`tests/sai-b059-invariants.mjs` fails if the 15 SHA-bound authorities, donor metrics, SHA ledger, 188 traversal, B059 workflow, hosted truth flags, portable boundary or active SAI binding disappear.
