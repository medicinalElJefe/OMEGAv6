# OMEGA Restore Manifest Addendum — Canon v24 R1

## Main base
`4c5b1b9b0eaf6e9f179c5944884fbb2b33a64c9b`

## Donor classification
- `CanonConsoleOmega v24 / v31r1`: GOVERNANCE / CANONICAL-STATE DONOR, not runtime authority.
- `atlas_runtime_test_bundle`: retained runtime/API authority.
- `Mode188_Atlas_Camera_Shell_v11`: retained renderer/camera donor.
- `Dewey_Full_PC_Runtime_188`: retained deep-logic donor.

## Exact donor semantics restored
One execution spine: observation → translation → atlas mapping → semantic state → transformation proof → face consistency → face repair → stewardship → counterfactual governance → multistate arbitration → federated consensus → replay validation → governance calibration.

The v24 single sovereign `CanonicalState` rule is retained. Candidate changes are parent-hashed, candidate-hashed, proof-scored, consensus-scored, replay-scored and calibration-gated. Browser proposals do not mutate release authority or Canon silently; explicit admission is required and can still be rejected by gates.

## Truth boundaries
Donor evidence verified Python compilation, core imports and non-GUI smoke execution. It did not verify a live webcam session or Windows EXE generation. Those capabilities remain outside hosted PASS.

## Surpass delta
The prior generic Governance / Canon Evolution panels are replaced by one source-bound control surface with deterministic proposal compilation, 13-stage telemetry, selected-corridor output, persistent proposal history and explicit admission semantics.

## Regression lock
`tests/canon-governance-invariants.mjs` fails if stages, single-state continuity, explicit admission, donor truth boundary or direct menu routing disappear.
