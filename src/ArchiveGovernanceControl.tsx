import {useMemo,useState} from 'react';
import {Archive,CheckCircle2,ChevronRight,Download,Search,ShieldAlert,ShieldCheck} from 'lucide-react';
import {ARCHIVE_DONORS,ARCHIVE_TRUTH_BOUNDARY,RECOVERY_RULES,archiveSummary,donorScore,recommendedDisposition,type ArchiveDisposition} from './archiveGovernanceRuntime';
import {localState} from './platformAdapter';
import ArchiveRecoveryQueueR48 from './ArchiveRecoveryQueueR48';
import ArchiveReconciliationR49 from './ArchiveReconciliationR49';
import './archiveGovernance.css';
const OPTIONS:ArchiveDisposition[]=['KEEP','DONOR','QUARANTINE','EXPAND'];
function exportJson(name:string,data:any){const b=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
export default function ArchiveGovernanceControl({operators=false}:{operators?:boolean}){const[q,setQ]=useState(''),[family,setFamily]=useState('ALL'),[overrides,setOverrides]=useState<Record<string,ArchiveDisposition>>(()=>localState.read('omega.v6.archive.governance.overrides',{}));const summary=archiveSummary(),families=['ALL',...new Set(ARCHIVE_DONORS.map(x=>x.family))],rows=useMemo(()=>ARCHIVE_DONORS.filter(x=>(family==='ALL'||x.family===family)&&`${x.name} ${x.role} ${x.evidence} ${x.lemmas.join(' ')}`.toLowerCase().includes(q.toLowerCase())),[q,family]);const set=(id:string,v:ArchiveDisposition)=>{const next={...overrides,[id]:v};setOverrides(next);localState.write('omega.v6.archive.governance.overrides',next)};const effective=(d:any)=>overrides[d.id]||recommendedDisposition(d);const exportReceipt=()=>exportJson('OMEGA_ARCHIVE_GOVERNANCE_RECEIPT.json',{generatedAt:new Date().toISOString(),summary,donors:ARCHIVE_DONORS.map(d=>({...d,score:donorScore(d),recommended:recommendedDisposition(d),effective:effective(d)})),recoveryRules:RECOVERY_RULES,boundary:ARCHIVE_TRUTH_BOUNDARY});return <section className='special-app archive-governance-app'><header className='special-head'><div><span>FORENSIC DONOR ADMISSION · {summary.total} CLASSIFIED DONOR FAMILIES</span><h2>{operators?'Archive Operators':'Archive Census'}</h2><p>Current hosted registry plus explicitly labeled historical cumulative-review evidence. This is not a live enumeration of every Drive file.</p></div><button className='primary-action' onClick={exportReceipt}><Download/>Receipt</button></header><ArchiveReconciliationR49/><div className='archive-summary'>{[['CLASSIFIED DONORS',summary.total],['EXPLICIT FILE COUNTS',summary.knownFileCount],['VERIFIED EXECUTION',summary.verified],['QUARANTINE',summary.quarantine],['EXPAND NEEDED',summary.expand],['HISTORICAL REVIEWED',summary.historicalReportedReviewedFiles],['HISTORICAL UNCLASSIFIED',summary.historicalReportedUnclassifiedCarriers]].map(([a,b])=><div key={String(a)}><span>{a}</span><b>{Number(b).toLocaleString()}</b></div>)}</div><div className='archive-search'><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder='Search donor, evidence, role or short lemma…'/><select value={family} onChange={e=>setFamily(e.target.value)}>{families.map(x=><option key={x}>{x}</option>)}</select></div><div className='archive-recovery-flow'><span>FINGERPRINT</span><ChevronRight/><span>CLASSIFY</span><ChevronRight/><span>AUTHORITY SCORE</span><ChevronRight/><span>ADAPT</span><ChevronRight/><span>PROVE</span><ChevronRight/><span>KEEP / DONOR / QUARANTINE / EXPAND</span></div>{operators&&<div className='recovery-rule-grid'>{RECOVERY_RULES.map(r=><article key={r.artifactClass}><b>{r.artifactClass}</b><span>{r.intent}</span><small>{r.rule}</small><strong>{r.allowed.join(' / ')}</strong><em>{r.action}</em></article>)}</div>}<div className='forensic-donor-grid'>{rows.map(d=>{const score=donorScore(d),rec=recommendedDisposition(d),eff=effective(d);return <article key={d.id} className={'donor-card '+eff.toLowerCase()}><header><div>{d.verification==='VERIFIED_EXECUTION'?<CheckCircle2/>:d.verification==='INVALID_CONTAINER'?<ShieldAlert/>:<Archive/>}<span><b>{d.name}</b><small>{d.family} · {d.verification}</small></span></div><strong>{Math.round(score*100)}%</strong></header><p>{d.role}</p><div className='donor-evidence'><span>EVIDENCE</span><p>{d.evidence}</p></div><div className='donor-metrics'><span>AUTH <b>{Math.round(d.authorityScore*100)}</b></span><span>PROOF <b>{Math.round(d.proofScore*100)}</b></span><span>PORTABLE <b>{Math.round(d.portabilityScore*100)}</b></span><span>RISK <b>{Math.round(d.regressionRisk*100)}</b></span></div><div className='lemma-row'>{d.lemmas.map(x=><code key={x}>{x}</code>)}</div><small className='donor-boundary'>{d.boundary}</small><footer><span>RECOMMENDED <b>{rec}</b></span>{operators?<div>{OPTIONS.map(v=><button key={v} className={eff===v?'active':''} onClick={()=>set(d.id,v)}>{v}</button>)}</div>:<strong>{eff}</strong>}</footer></article>})}</div>{operators&&<ArchiveRecoveryQueueR48/>}<div className='special-boundary'><ShieldCheck/>{ARCHIVE_TRUTH_BOUNDARY}</div></section>}
