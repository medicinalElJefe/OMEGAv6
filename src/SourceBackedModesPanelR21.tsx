import {useMemo,useState} from 'react';
import {Activity,ArrowRight,Calculator,ChevronRight,Database,GitBranch,Search,ShieldCheck,Waypoints} from 'lucide-react';
import {evaluateCorpusModes} from './corpusRuntime';
import {compileSourceTraversal,R21_MODE_AUTHORITY,sourceBackedModeSummary,type SourceBackedModeResult} from './sourceBackedModeRuntimeR21';
import './sourceBackedModesR21.css';

type Props={record:any;address:number;onAddress:(address:number)=>void;onNavigate:(panel:string)=>void};
const fmt=(v:any,d=4)=>Number.isFinite(Number(v))?Number(v).toFixed(d):String(v??'—');
const statusLabel=(x:SourceBackedModeResult)=>x.state==='EXECUTED_EXACT'?'EXECUTED EXACT':x.state==='SOURCE_PACKET'?'SOURCE PACKET':x.state==='DERIVED_RUNTIME'?'DERIVED RUNTIME':'GATED';

export default function SourceBackedModesPanelR21({record,address,onAddress,onNavigate}:Props){
  const summary=useMemo(()=>sourceBackedModeSummary(record),[record]),catalog=useMemo(()=>evaluateCorpusModes(record),[record]),route=useMemo(()=>compileSourceTraversal(address,72),[address]),[query,setQuery]=useState(''),[showCatalog,setShowCatalog]=useState(false);
  const q=query.trim().toLowerCase(),catalogRows=catalog.results.filter((x:any)=>!q||`${x.name} ${x.category} ${x.operator} ${x.algebra} ${x.calculus}`.toLowerCase().includes(q));
  const points=route.path.map((x,i)=>{const px=18+(i/Math.max(1,route.path.length-1))*964,pressure=Math.max(0,Math.min(1,(x.q+x.Lambda)/2)),score=Math.max(0,Math.min(1,x.dewey/(1+x.dewey)));return{x,px,py:220-score*168,pressure}}),line=points.map(p=>`${p.px.toFixed(1)},${p.py.toFixed(1)}`).join(' ');
  const goStep=(n:number)=>{const p=route.path[Math.max(0,Math.min(route.path.length-1,n))];if(p)onAddress(p.address)};
  return <section className='special-app sbm21-app'>
    <header className='special-head'><div><span>SOURCE-BACKED CALCULUS · NO SEMANTIC-AFFINITY EXECUTION CLAIMS</span><h2>Modes + Applied Calculus</h2></div><strong>{summary.appliedCount} APPLIED · {summary.gatedCount} GATED</strong></header>
    <div className='sbm21-truth'><ShieldCheck/><div><b>179 catalog entries are loaded as source metadata, not automatically called “executed.”</b><span>{summary.exactCount} exact operators execute from current packet inputs · {summary.packetCount} source/derived runtime channels are exposed directly · missing inputs remain gated.</span></div></div>
    <div className='sbm21-kpis'><div><span>STATE</span><b>{record.stateId}</b><small>D{record.coordinates.d+1} · P{record.coordinates.p+1} · R{record.coordinates.r+1} · L{record.coordinates.l+1}</small></div><div><span>DEWEY S</span><b>{fmt(summary.executed.find(x=>x.id==='M001')?.value)}</b><small>(CΩ·Φ)/(q+Λ+ε)</small></div><div><span>MODE 188</span><b>{fmt(summary.executed.find(x=>x.id==='M002')?.value)}</b><small>{String(record.metrics.mode188)}</small></div><div><span>DECISION</span><b>{record.metrics.decision}</b><small>next state {Number(record.autoPing.dataNext)+1}</small></div></div>
    <section className='sbm21-route'><header><div><Waypoints/><span>ACTUAL ADMITTED TRAVERSAL</span><b>{route.path.length} source states</b></div><div className='sbm21-route-actions'><button onClick={()=>onAddress(record.autoPing.previous)}>Previous</button><button onClick={()=>onAddress(record.autoPing.dataNext)}>Admitted next <ArrowRight/></button><button onClick={()=>goStep(12)}>Follow 12</button><button onClick={()=>onNavigate('Visual Instrument')}>Visual Instrument</button><button onClick={()=>onNavigate('Traversal')}>Traversal</button></div></header><svg viewBox='0 0 1000 250' role='img' aria-label='Canonical source traversal with Dewey score'><line x1='18' y1='220' x2='982' y2='220' className='sbm21-axis'/><polyline points={line} className='sbm21-route-line'/>{points.map((p,i)=><g key={`${p.x.address}-${i}`} className={`sbm21-node ${p.x.decision.toLowerCase()}`} onClick={()=>onAddress(p.x.address)}><circle cx={p.px} cy={p.py} r={i===0?7:4.5}/>{i%8===0&&<text x={p.px} y={Math.max(14,p.py-10)} textAnchor='middle'>{p.x.stateId}</text>}</g>)}</svg><footer><span>vertical = bounded Dewey score S/(1+S)</span><span>route = exact autoPing.dataNext sequence</span><b>{route.closed?'LOOP/CLOSURE DETECTED':'OPEN TRAVERSAL'}</b></footer></section>
    <div className='sbm21-mode-columns'><section><header><Calculator/><b>EXECUTED / SOURCE-BOUND OPERATORS</b><span>{summary.appliedCount}</span></header><div className='sbm21-mode-list'>{summary.rows.filter(x=>x.state!=='GATED_MISSING_INPUTS').map(x=><article key={x.id} className={x.state.toLowerCase()}><code>{x.id}</code><div><b>{x.name}</b><small>{x.formula}</small><em>{x.source}</em><p>{x.detail}</p></div><strong>{statusLabel(x)}<span>{x.value===null?'—':String(x.value)}</span></strong></article>)}</div></section><section><header><ShieldCheck/><b>FORMULAS HELD FOR MISSING INPUTS</b><span>{summary.gatedCount}</span></header><div className='sbm21-mode-list gated'>{summary.gated.map(x=><article key={x.id}><code>{x.id}</code><div><b>{x.name}</b><small>{x.formula}</small><em>{x.source}</em><p>Missing authoritative inputs: {x.missing.join(', ')}</p></div><strong>GATED</strong></article>)}</div></section></div>
    <section className='sbm21-catalog'><header><div><Database/><b>179-SOURCE MODE CATALOG</b><span>metadata, operators, algebra and proof requirements</span></div><div><label><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder='Search mode source registry…'/></label><button onClick={()=>setShowCatalog(v=>!v)}>{showCatalog?'Hide':'Inspect'} catalog</button></div></header>{showCatalog&&<div className='sbm21-catalog-grid'>{catalogRows.map((x:any)=><details key={x.id}><summary><code>{x.id}</code><span><b>{x.name}</b><small>{x.category}</small></span><strong>CATALOG</strong><ChevronRight/></summary><div><p><b>Operator</b> {x.operator||'—'}</p><p><b>Algebra</b> {x.algebra||'—'}</p><p><b>Calculus</b> {x.calculus||'—'}</p><p><b>Proof requirement</b> {x.proof||'—'}</p><p><b>Ledger</b> {x.ledger||'—'}</p><em>Legacy semantic affinity score/gate is intentionally not displayed as execution.</em></div></details>)}</div>}</section>
    <div className='sbm21-actions'><button onClick={()=>onNavigate('Relativity')}><GitBranch/>Dimensional Relativity</button><button onClick={()=>onNavigate('Evidence & Proof')}><ShieldCheck/>Evidence & Proof</button><button onClick={()=>onNavigate('System Atlas')}><Database/>Software Atlas</button><button onClick={()=>onNavigate('Control Matrix')}><Activity/>Control Matrix</button></div>
    <div className='special-boundary'><ShieldCheck/>{R21_MODE_AUTHORITY.boundary} {route.boundary}</div>
  </section>
}
