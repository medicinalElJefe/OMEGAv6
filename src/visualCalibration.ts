import {STATE_COUNT,corpusState} from './corpusRuntime';import {RUNTIME_IDENTITY} from './runtimeIdentity';
export type CalibrationStat={id:string;label:string;group:string;min:number;p02:number;p50:number;p98:number;max:number;mean:number;std:number;finite:number};export type VisualCalibration={build:string;stateCount:number;channelCount:number;stats:Record<string,CalibrationStat>;ordered:CalibrationStat[];passed:boolean;elapsedMs:number;boundary:string};type Def={id:string;label:string;group:string;get:(r:any)=>number};
const cl=(x:number)=>Math.max(0,Math.min(1,Number.isFinite(x)?x:0)),gm3=(a:number,b:number,c:number)=>Math.cbrt(Math.max(1e-12,a)*Math.max(1e-12,b)*Math.max(1e-12,c));
export const CALIBRATION_CHANNELS:Def[]=[{id:'C',label:'Continuity CΩ',group:'Canon',get:r=>r.metrics.continuity},{id:'Phi',label:'Future plasticity Φ',group:'Canon',get:r=>r.metrics.plasticity},{id:'q',label:'Contradiction q',group:'Canon',get:r=>r.metrics.contradiction},{id:'Lambda',label:'Burden Λ',group:'Canon',get:r=>r.metrics.burden},{id:'scar',label:'Scar',group:'Canon',get:r=>r.metrics.scar},{id:'evidence',label:'Evidence',group:'Proof',get:r=>r.metrics.evidence},{id:'rsc',label:'RSC-relational total',group:'Relational',get:r=>r.metrics.rsc},{id:'geometry',label:'Geometry coherence',group:'Geometry',get:r=>r.metrics.geometry},{id:'stability',label:'Common kernel S',group:'Canon',get:r=>r.metrics.stability},{id:'compatibility',label:'Compatibility',group:'Predictive',get:r=>r.predict.compatibility},{id:'carry',label:'Carry',group:'Predictive',get:r=>r.predict.carry},{id:'reconstructability',label:'Reconstructability',group:'Predictive',get:r=>r.predict.reconstructability},{id:'dOmega',label:'DΩ',group:'Predictive',get:r=>r.predict.dOmega},{id:'integralCarry',label:'Integral carry',group:'Predictive',get:r=>r.predict.integralCarry},{id:'curvature',label:'Curvature',group:'Predictive',get:r=>r.predict.curvature},{id:'forecastScore',label:'Forecast score',group:'Forecast',get:r=>r.predict.forecastScore},{id:'dPhi',label:'ΔΦ',group:'Recoverability',get:r=>r.phi.dPhi},{id:'positiveNeighbors',label:'Positive Φ neighbors',group:'Recoverability',get:r=>r.phi.positiveNeighbors},{id:'sPhi',label:'SΦ',group:'Recoverability',get:r=>r.phi.sPhi},{id:'uncertainty',label:'Uncertainty',group:'Proof',get:r=>r.identity.uncertainty},{id:'weakestLink',label:'Weakest link',group:'Proof',get:r=>r.identity.weakestLink},{id:'shapeResponse',label:'Shape response',group:'Relativity',get:r=>r.math.shapeResponse},{id:'motionRelativity',label:'Motion relativity',group:'Relativity',get:r=>r.math.motionRelativity},{id:'normalizedMotionRelativity',label:'Normalized MRI',group:'Relativity',get:r=>r.math.normalizedMotionRelativity},{id:'autoPingGain',label:'AutoPing gain',group:'Relativity',get:r=>r.math.autoPingGain},{id:'parentScore',label:'Parent score',group:'RSC',get:r=>r.psc.parentScore},{id:'accumulationScore',label:'Accumulation score',group:'RSC',get:r=>r.psc.accumulationScore},{id:'constraintScore',label:'Constraint score',group:'RSC',get:r=>r.psc.constraintScore},{id:'pscScar',label:'PSC scar',group:'RSC',get:r=>r.psc.scarScore},{id:'phaseScore',label:'PSC phase',group:'RSC',get:r=>r.psc.phaseScore},{id:'continuityScore',label:'PSC continuity',group:'RSC',get:r=>r.psc.continuityScore},{id:'pscMotion',label:'PSC motion relativity',group:'RSC',get:r=>r.psc.motionRelativityScore},{id:'totalScore',label:'PSC total',group:'RSC',get:r=>r.psc.totalScore},{id:'compressionRatio',label:'Compression ratio',group:'RSC',get:r=>r.psc.compressionRatio},{id:'symmetry',label:'Symmetry',group:'Geometry',get:r=>r.geometry.symmetry},{id:'asymmetry',label:'Asymmetry',group:'Geometry',get:r=>r.geometry.asymmetry},{id:'petal',label:'Petal response',group:'Geometry',get:r=>r.geometry.petal},{id:'toroid',label:'Toroid return',group:'Geometry',get:r=>r.geometry.toroid},{id:'compression',label:'Geometry compression',group:'Geometry',get:r=>r.geometry.compression},{id:'inverseReadiness',label:'Inverse readiness',group:'Geometry',get:r=>r.geometry.inverseReadiness},{id:'radius',label:'Source radius',group:'Geometry',get:r=>r.geometry.radius},{id:'geoX',label:'Source X',group:'Geometry',get:r=>r.geometry.x},{id:'geoY',label:'Source Y',group:'Geometry',get:r=>r.geometry.y},{id:'theta',label:'θ',group:'Geometry',get:r=>r.geometry.theta},{id:'phiAngle',label:'φ',group:'Geometry',get:r=>r.geometry.phi},{id:'inverseX',label:'Inverse X',group:'Geometry',get:r=>r.geometry.inverseX},{id:'inverseY',label:'Inverse Y',group:'Geometry',get:r=>r.geometry.inverseY},{id:'mode188Ratio',label:'Mode188 ratio',group:'Unified calculus',get:r=>cl(r.metrics.continuity)/(cl(r.metrics.burden)+cl(r.metrics.contradiction)+.35*cl(r.metrics.burden)*cl(r.metrics.contradiction)+.05)},{id:'waterKernel',label:'Water conductance kernel',group:'Unified calculus',get:r=>cl((cl(r.metrics.continuity)+cl(r.metrics.plasticity)+(1-cl(r.metrics.burden))+(1-cl(r.metrics.contradiction))+cl(r.metrics.rsc))/5)},{id:'triangulationKernel',label:'Triangulation survival',group:'Unified calculus',get:r=>gm3(cl(r.metrics.evidence),cl(r.metrics.rsc),cl(r.metrics.geometry)))},{id:'occupancyKernel',label:'Occupancy ρ',group:'Unified calculus',get:r=>cl(.30*r.metrics.continuity+.17*r.metrics.plasticity+.23*r.metrics.geometry+.15*(1-r.metrics.burden)+.15*(1-r.metrics.scar))},{id:'proofScarKernel',label:'Proof scar',group:'Unified calculus',get:r=>cl(((1-r.metrics.evidence)+r.metrics.contradiction+r.metrics.scar)/3)},{id:'routeDelta',label:'Source route Δ',group:'Unified calculus',get:r=>(Number(r.autoPing.dataNext)-Number(r.address))/STATE_COUNT}];
let cached:VisualCalibration|null=null,pending:Promise<VisualCalibration>|null=null;const q=(a:Float64Array,p:number)=>a[Math.max(0,Math.min(a.length-1,Math.round((a.length-1)*p)))];export function buildVisualCalibration():VisualCalibration{if(cached)return cached;const started=performance.now(),arrays=CALIBRATION_CHANNELS.map(()=>new Float64Array(STATE_COUNT)),sum=new Float64Array(CALIBRATION_CHANNELS.length),sum2=new Float64Array(CALIBRATION_CHANNELS.length),finite=new Uint32Array(CALIBRATION_CHANNELS.length);for(let a=0;a<STATE_COUNT;a++){const r=corpusState(a);for(let i=0;i<CALIBRATION_CHANNELS.length;i++){let v=Number(CALIBRATION_CHANNELS[i].get(r));if(!Number.isFinite(v))v=0;else finite[i]++;arrays[i][a]=v;sum[i]+=v;sum2[i]+=v*v}}const ordered=CALIBRATION_CHANNELS.map((d,i)=>{const a=arrays[i];a.sort();const mean=sum[i]/STATE_COUNT,std=Math.sqrt(Math.max(0,sum2[i]/STATE_COUNT-mean*mean));return{id:d.id,label:d.label,group:d.group,min:a[0],p02:q(a,.02),p50:q(a,.5),p98:q(a,.98),max:a[a.length-1],mean,std,finite:finite[i]}}),stats=Object.fromEntries(ordered.map(x=>[x.id,x])),passed=ordered.every(x=>x.finite===STATE_COUNT&&Number.isFinite(x.p02)&&Number.isFinite(x.p98)&&x.max>=x.min);cached={build:RUNTIME_IDENTITY.hostedBuild,stateCount:STATE_COUNT,channelCount:ordered.length,stats,ordered,passed,elapsedMs:performance.now()-started,boundary:'Calibration is source-relative and computed across every one of the 20,736 embedded states for every bound visual/calculus channel. It improves internal comparability; it does not create external physical units or fabricate missing W/A⁻/γ calibration.'};return cached}async function buildVisualCalibrationCooperative():Promise<VisualCalibration>{if(cached)return cached;const started=performance.now(),arrays=CALIBRATION_CHANNELS.map(()=>new Float64Array(STATE_COUNT)),sum=new Float64Array(CALIBRATION_CHANNELS.length),sum2=new Float64Array(CALIBRATION_CHANNELS.length),finite=new Uint32Array(CALIBRATION_CHANNELS.length),lowPower=typeof window!=='undefined'&&(window.matchMedia('(max-width:760px)').matches||(navigator.hardwareConcurrency||8)<=4),chunk=lowPower?512:1024,yieldUi=()=>new Promise<void>(resolve=>setTimeout(resolve,0));for(let start=0;start<STATE_COUNT;start+=chunk){const end=Math.min(STATE_COUNT,start+chunk);for(let a=start;a<end;a++){const r=corpusState(a);for(let i=0;i<CALIBRATION_CHANNELS.length;i++){let v=Number(CALIBRATION_CHANNELS[i].get(r));if(!Number.isFinite(v))v=0;else finite[i]++;arrays[i][a]=v;sum[i]+=v;sum2[i]+=v*v}}if(end<STATE_COUNT)await yieldUi()}const ordered:CalibrationStat[]=[];for(let i=0;i<CALIBRATION_CHANNELS.length;i++){const d=CALIBRATION_CHANNELS[i],a=arrays[i];a.sort();const mean=sum[i]/STATE_COUNT,std=Math.sqrt(Math.max(0,sum2[i]/STATE_COUNT-mean*mean));ordered.push({id:d.id,label:d.label,group:d.group,min:a[0],p02:q(a,.02),p50:q(a,.5),p98:q(a,.98),max:a[a.length-1],mean,std,finite:finite[i]});if((i+1)%3===0&&i+1<CALIBRATION_CHANNELS.length)await yieldUi()}const stats=Object.fromEntries(ordered.map(x=>[x.id,x])),passed=ordered.every(x=>x.finite===STATE_COUNT&&Number.isFinite(x.p02)&&Number.isFinite(x.p98)&&x.max>=x.min);cached={build:RUNTIME_IDENTITY.hostedBuild,stateCount:STATE_COUNT,channelCount:ordered.length,stats,ordered,passed,elapsedMs:performance.now()-started,boundary:'Calibration is source-relative and computed across every one of the 20,736 embedded states for every bound visual/calculus channel. It improves internal comparability; it does not create external physical units or fabricate missing W/A⁻/γ calibration.'};return cached}export function getVisualCalibration(){if(cached)return Promise.resolve(cached);if(!pending)pending=buildVisualCalibrationCooperative().catch(e=>{pending=null;throw e});return pending}export function calibratedValue(cal:VisualCalibration,id:string,v:number){const s=cal.stats[id];if(!s||!Number.isFinite(v))return 0;const lo=s.p02,hi=s.p98;if(Math.abs(hi-lo)<1e-12)return .5;return Math.max(0,Math.min(1,(v-lo)/(hi-lo)))}export function calibratedState(r:any,cal:VisualCalibration){const n=(id:string,v:number)=>calibratedValue(cal,id,Number(v)),C=n('C',r.metrics.continuity),Phi=n('Phi',r.metrics.plasticity),qv=n('q',r.metrics.contradiction),L=n('Lambda',r.metrics.burden),scar=n('scar',r.metrics.scar),evidence=n('evidence',r.metrics.evidence),rsc=n('rsc',r.metrics.rsc),sym=n('symmetry',r.geometry.symmetry),asym=n('asymmetry',r.geometry.asymmetry),petal=n('petal',r.geometry.petal),toroid=n('toroid',r.geometry.toroid),inverse=n('inverseReadiness',r.geometry.inverseReadiness),compression=n('compression',r.geometry.compression),motion=n('normalizedMotionRelativity',r.math.normalizedMotionRelativity),gain=n('autoPingGain',r.math.autoPingGain),carry=n('carry',r.predict.carry),curvature=n('curvature',r.predict.curvature),compat=n('compatibility',r.predict.compatibility),recon=n('reconstructability',r.predict.reconstructability),forecast=n('forecastScore',r.predict.forecastScore),weak=n('weakestLink',r.identity.weakestLink),pscContinuity=n('continuityScore',r.psc.continuityScore),parent=n('parentScore',r.psc.parentScore),constraint=n('constraintScore',r.psc.constraintScore),pscPhase=n('phaseScore',r.psc.phaseScore),pscMotion=n('pscMotion',r.psc.motionRelativityScore),mode188=n('mode188Ratio',cl(r.metrics.continuity)/(cl(r.metrics.burden)+cl(r.metrics.contradiction)+.35*cl(r.metrics.burden)*cl(r.metrics.contradiction)+.05)),waterKernel=n('waterKernel',cl((cl(r.metrics.continuity)+cl(r.metrics.plasticity)+(1-cl(r.metrics.burden))+(1-cl(r.metrics.contradiction))+cl(r.metrics.rsc))/5)),triangulation=n('triangulationKernel',gm3(cl(r.metrics.evidence),cl(r.metrics.rsc),cl(r.metrics.geometry))),occupancy=n('occupancyKernel',cl(.30*r.metrics.continuity+.17*r.metrics.plasticity+.23*r.metrics.geometry+.15*(1-r.metrics.burden)+.15*(1-r.metrics.scar))),proofScar=n('proofScarKernel',cl(((1-r.metrics.evidence)+r.metrics.contradiction+r.metrics.scar)/3)),routeDelta=n('routeDelta',(Number(r.autoPing.dataNext)-Number(r.address))/STATE_COUNT),mean=(...x:number[])=>x.reduce((a,b)=>a+b,0)/x.length,gm=(x:number[])=>Math.pow(x.reduce((a,b)=>a*Math.max(1e-9,b),1),1/x.length);return{C,Phi,q:qv,Lambda:L,scar,evidence,rsc,symmetry:sym,asymmetry:asym,petal,toroid,inverse,compression,motion,gain,carry,curvature,compatibility:compat,reconstructability:recon,forecast,weakest:weak,pscContinuity,parent,constraint,pscPhase,pscMotion,mode188,waterKernel,triangulation,occupancy,proofScar,routeDelta,water:mean(C,Phi,1-L,1-qv,rsc,pscContinuity,waterKernel),light:mean(evidence,compat,recon,sym,C,triangulation),proof:gm([evidence,weak,1-qv,1-L,1-proofScar]),relativity:mean(motion,gain,pscMotion),shape:mean(sym,petal,toroid,1-compression,rsc,occupancy),pressure:mean(qv,L,scar,asym,constraint,proofScar),future:mean(Phi,forecast,evidence,recon),topology:mean(parent,pscContinuity,pscPhase,rsc,triangulation),inverseField:mean(inverse,recon,compat),outverseField:mean(C,Phi,1-L,sym),routeStrength:mean(mode188,routeDelta,triangulation)}}
