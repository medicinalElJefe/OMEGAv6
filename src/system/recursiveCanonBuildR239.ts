export const R239_SCHEMA='OMEGA_RECURSIVE_CANON_BUILD_FABRIC_R239' as const;
export const R239_REVISION='R239' as const;
export const R239_CONTINUITY_OPERATOR='PARTITION → EXCHANGE/TRANSFORM → INVARIANT CARRY → SCAR/RESIDUAL CARRY → RE-CONTEXTUALIZE/REPARTITION' as const;
export const R239_RESOLUTION=Object.freeze({organs:12,branches:144,cells:1728,lanes:20736,deepAddress:248832});
export const R239_ORGANS=Object.freeze(['CANON_KERNEL','STATE_MEMORY','APPLIED_CALCULUS','EVIDENCE_WORLD','HYBRID_COMPUTE','CLOUD_FABRIC','INTELLIGENCE','SWARM_ORGANISM','PROOF_GOVERNANCE','RENDER_PROJECTION','OPERATOR_INTERFACE','EVOLUTION_BUILD'] as const);
export type R239Organ=typeof R239_ORGANS[number];
export type R239Risk='LOW'|'MEDIUM'|'HIGH'|'CRITICAL';
export type R239CellState='DORMANT'|'READY'|'ACTIVE'|'RETURNED'|'VERIFIED'|'BLOCKED'|'REJECTED';
export type R239Orientation=-1|0|1;
export type R239InvariantEnvelope={authority:string[];schemas:string[];identity:string[];provenance:string[];continuity:string[];truthBoundaries:string[];interfaces:string[]};
export type R239PressureInput={contradiction:number;burden:number;residual:number;userValueDeficit:number;bottleneck:number;continuity:number};
export type R239WorkCell={id:string;address:number;laneStart:number;laneEnd:number;organ:R239Organ;branch:number;cell:number;objective:string;dependencies:string[];invariants:string[];files:string[];tests:string[];expectedGain:number;complexity:number;contradictionRisk:number;risk:R239Risk;orientation:R239Orientation;state:R239CellState;pressure:R239PressureInput;canonicalMutation:false};
export type R239MergeReceipt={cellId:string;baseSha:string;candidateSha:string;invariantsPreserved:boolean;tests:Record<string,boolean>;residualGate:'PASS'|'BLOCK'|'UNPROVEN';returned:boolean;verified:boolean;canonicalAdmission:false};
const clamp01=(n:number)=>Math.max(0,Math.min(1,Number.isFinite(n)?n:0));
export function r239CellAddress(organ:number,branch:number,cell:number){if(!Number.isInteger(organ)||organ<0||organ>=12)throw new Error('R239 organ index must be 0..11');if(!Number.isInteger(branch)||branch<0||branch>=12)throw new Error('R239 branch index must be 0..11');if(!Number.isInteger(cell)||cell<0||cell>=12)throw new Error('R239 cell index must be 0..11');return organ*144+branch*12+cell}
export function r239LaneRange(cellAddress:number){if(!Number.isInteger(cellAddress)||cellAddress<0||cellAddress>=1728)throw new Error('R239 cell address must be 0..1727');const laneStart=cellAddress*12;return{laneStart,laneEnd:laneStart+11}}
export function r239Pressure(x:R239PressureInput){const q=clamp01(x.contradiction),burden=clamp01(x.burden),residual=clamp01(x.residual),value=clamp01(x.userValueDeficit),bottleneck=clamp01(x.bottleneck),continuity=clamp01(x.continuity);return clamp01(.22*q+.16*burden+.22*residual+.14*value+.18*bottleneck+.08*(1-continuity))}
export function r239CandidateScore(cell:Pick<R239WorkCell,'expectedGain'|'complexity'|'contradictionRisk'|'risk'|'pressure'>){const riskFactor=cell.risk==='LOW'?1:cell.risk==='MEDIUM'?.7:cell.risk==='HIGH'?.35:.1;const structuralGain=clamp01(cell.expectedGain)*(0.55+0.45*r239Pressure(cell.pressure));return structuralGain/Math.max(.01,clamp01(cell.complexity)+clamp01(cell.contradictionRisk))*riskFactor}
export function r239ReadyFrontier(cells:R239WorkCell[],completed:Iterable<string>,maxParallel=12){const done=new Set(completed);const width=Math.max(1,Math.min(144,Math.floor(maxParallel)||1));return cells.filter(cell=>cell.state!=='REJECTED'&&cell.state!=='BLOCKED'&&cell.dependencies.every(dep=>done.has(dep))).sort((a,b)=>r239CandidateScore(b)-r239CandidateScore(a)||b.expectedGain-a.expectedGain||a.address-b.address).slice(0,width)}
export function r239MergeAdmissible(receipt:R239MergeReceipt){return Boolean(receipt.baseSha&&receipt.candidateSha&&receipt.invariantsPreserved&&receipt.residualGate==='PASS'&&receipt.returned&&receipt.verified&&Object.values(receipt.tests).length>0&&Object.values(receipt.tests).every(Boolean)&&receipt.canonicalAdmission===false)}
export function r239CreateCell(input:Omit<R239WorkCell,'address'|'laneStart'|'laneEnd'|'canonicalMutation'>):R239WorkCell{const organIndex=R239_ORGANS.indexOf(input.organ);if(organIndex<0)throw new Error(`Unknown R239 organ ${input.organ}`);const address=r239CellAddress(organIndex,input.branch,input.cell);const{laneStart,laneEnd}=r239LaneRange(address);return{...input,address,laneStart,laneEnd,canonicalMutation:false}}
export function recursiveCanonBuildR239(cells:R239WorkCell[],completed:Iterable<string>,maxParallel=12){const frontier=r239ReadyFrontier(cells,completed,maxParallel);return{schema:R239_SCHEMA,revision:R239_REVISION,continuityOperator:R239_CONTINUITY_OPERATOR,resolution:R239_RESOLUTION,organs:R239_ORGANS,sparseActivation:true,frontier,activeCellAddresses:frontier.map(x=>x.address),activeLaneRanges:frontier.map(x=>[x.laneStart,x.laneEnd] as const),admissionLaw:'MAXIMUM INTERNAL EVOLUTION + STRICT EXTERNAL ADMISSION',executionBoundary:'R239 schedules and proves bounded work cells; it does not directly mutate main, dispatch unapproved native work, or admit CanonState.',canonicalAdmissionAuthority:'R125',dispatchAuthority:'R147',hybridReturnAuthority:'R141',durableHistoryAuthority:'R146'}}
