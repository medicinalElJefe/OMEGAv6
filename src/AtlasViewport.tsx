import {useEffect,useMemo,useRef,useState} from 'react';
import {Pause,Play,RotateCcw,Save,StepBack,StepForward} from 'lucide-react';
import {PROJECTIONS,Projection,STATE_COUNT,VIEW_MODES,ViewMode,corpusState,decodeAddress,projectionPoint,sourceRGB} from './corpusRuntime';
import {sourceBackedModeSummary} from './sourceBackedModeRuntimeR21';
import './atlasR36.css';

type Props={state:any;onSelect:(coords:{d:number;p:number;r:number;l:number})=>void};
const SIZE=900,cl=(x:number)=>Math.max(0,Math.min(1,x));
export default function AtlasViewport({state,onSelect}:Props){const ref=useRef<HTMLCanvasElement|null>(null),canonical=Number(state?.atlas?.address??0),[cursor,setCursor]=useState(canonical),[playing,setPlaying]=useState(false),[view,setView]=useState<ViewMode>('SOURCE_COLOR'),[projection,setProjection]=useState<Projection>('MANDALA'),[focus,setFocus]=useState(.72),[spin,setSpin]=useState(true);useEffect(()=>{if(!playing)setCursor(canonical)},[canonical,playing]);const record=useMemo(()=>corpusState(cursor),[cursor]),modeRun=useMemo(()=>sourceBackedModeSummary(record),[record]);const move=(a:number)=>setCursor(Math.max(0,Math.min(STATE_COUNT-1,Math.floor(a))));useEffect(()=>{if(!playing)return;const id=window.setInterval(()=>setCursor(a=>corpusState(a).autoPing.dataNext),520);return()=>window.clearInterval(id)},[playing]);
 useEffect(()=>{const c=ref.current;if(!c)return;let raf=0,alive=true;const ctx=c.getContext('2d');if(!ctx)return;const draw=(now:number)=>{if(!alive)return;const box=c.getBoundingClientRect(),dpr=Math.min(1.6,window.devicePixelRatio||1),W=Math.max(460,Math.round(box.width)),H=Math.max(500,Math.round(box.height));if(c.width!==Math.round(W*dpr)||c.height!==Math.round(H*dpr)){c.width=Math.round(W*dpr);c.height=Math.round(H*dpr)}ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,W,H);ctx.fillStyle='#02070a';ctx.fillRect(0,0,W,H);const sx=W/SIZE,sy=H/SIZE,t=spin?now*.00008:0,active=decodeAddress(cursor);const stride=W<700?4:2;for(let a=0;a<STATE_COUNT;a+=stride){const p=projectionPoint(a,projection,SIZE),cc=decodeAddress(a),[r,g,b]=sourceRGB(a,view),sameD=cc.d===active.d,sameP=cc.p===active.p,depth=(cc.l+1)/12,near=(sameD?1:.18)*(sameP?.7:.35),alpha=.025+.23*cl(focus*near+.22*depth),cx=(p.x-SIZE/2),cy=(p.y-SIZE/2),rot=t*(.16+.025*cc.p),cs=Math.cos(rot),sn=Math.sin(rot),x=W/2+(cx*cs-cy*sn)*sx,y=H/2+(cx*sn+cy*cs)*sy,z=.75+.55*depth,rad=.45+1.35*z*(sameD?1.2:1);ctx.fillStyle=`rgba(${r},${g},${b},${alpha})`;ctx.beginPath();ctx.arc(x,y,rad,0,Math.PI*2);ctx.fill()}
 const markers=[{a:record.autoPing.dataNext,c:'#58dfcd',r:6},{a:record.autoPing.hourglassMirror,c:'#d7aa5f',r:5},{a:record.autoPing.oppositeDomain,c:'#d56a78',r:5},{a:cursor,c:'#fff1c3',r:9}];markers.forEach(m=>{const p=projectionPoint(m.a,projection,SIZE),cx=p.x-SIZE/2,cy=p.y-SIZE/2,rot=t*(.16+.025*decodeAddress(m.a).p),cs=Math.cos(rot),sn=Math.sin(rot),x=W/2+(cx*cs-cy*sn)*sx,y=H/2+(cx*sn+cy*cs)*sy;ctx.fillStyle=m.c;ctx.beginPath();ctx.arc(x,y,m.r,0,Math.PI*2);ctx.fill();ctx.strokeStyle=m.c+'66';ctx.beginPath();ctx.arc(x,y,m.r+7+Math.sin(now*.004)*2,0,Math.PI*2);ctx.stroke()});
 ctx.font='11px ui-monospace,monospace';ctx.fillStyle='rgba(218,234,231,.8)';ctx.fillText(`ATLAS · ${projection} · ${view} · STATE ${record.stateId}`,16,24);ctx.fillStyle='rgba(126,151,155,.72)';ctx.fillText(`D${active.d+1}/P${active.p+1}/R${active.r+1}/L${active.l+1} · ${modeRun.appliedCount} source-backed applied · ${modeRun.gatedCount} gated`,16,43);raf=requestAnimationFrame(draw)};raf=requestAnimationFrame(draw);return()=>{alive=false;cancelAnimationFrame(raf)}},[cursor,projection,view,focus,spin,record,modeRun]);
 const click=(e:React.MouseEvent<HTMLCanvasElement>)=>{const c=e.currentTarget,rect=c.getBoundingClientRect(),x=(e.clientX-rect.left)/rect.width*SIZE,y=(e.clientY-rect.top)/rect.height*SIZE;let best=0,dist=Infinity;for(let a=0;a<STATE_COUNT;a++){const p=projectionPoint(a,projection,SIZE),d=(p.x-x)**2+(p.y-y)**2;if(d<dist){dist=d;best=a}}setPlaying(false);move(best)};
 return <div className='atlas-r36'><div className='atlas-r36-toolbar'><div>{PROJECTIONS.map(x=><button key={x} className={projection===x?'active':''} onClick={()=>setProjection(x)}>{x}</button>)}</div><div>{VIEW_MODES.map(x=><button key={x} className={view===x?'active':''} onClick={()=>setView(x)}>{x.replaceAll('_',' ')}</button>)}</div><label>focus<input type='range' min='.15' max='1' step='.05' value={focus} onChange={e=>setFocus(Number(e.target.value))}/></label></div><div className='atlas-r36-stage'><canvas ref={ref} onClick={click}/></div><div className='atlas-r36-actions'><button onClick={()=>setPlaying(v=>!v)}>{playing?<Pause/>:<Play/>}{playing?'Pause traversal':'Traverse atlas'}</button><button onClick={()=>move(record.autoPing.previous)}><StepBack/>Previous</button><button onClick={()=>move(record.autoPing.dataNext)}><StepForward/>Admitted next</button><button onClick={()=>setSpin(v=>!v)}>{spin?'Freeze field':'Animate field'}</button><button onClick={()=>{setPlaying(false);move(canonical)}}><RotateCcw/>Canonical</button><button className='gold' onClick={()=>{setPlaying(false);onSelect(decodeAddress(cursor))}}><Save/>Commit state</button></div><div className='atlas-r36-readout'><article><span>Current</span><b>{record.stateId}</b><small>{record.metrics.decision}</small></article><article><span>Continuity / plasticity</span><b>{record.metrics.continuity.toFixed(3)} / {record.metrics.plasticity.toFixed(3)}</b></article><article><span>Contradiction / burden</span><b>{record.metrics.contradiction.toFixed(3)} / {record.metrics.burden.toFixed(3)}</b></article><article><span>Source-backed execution</span><b>{modeRun.appliedCount} applied</b><small>{modeRun.gatedCount} gated · Catalog membership is not execution.</small></article></div></div>;
}
