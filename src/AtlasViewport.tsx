import {useEffect,useMemo,useRef,useState} from 'react';
import {Pause,Play,RotateCcw,Save,StepBack,StepForward} from 'lucide-react';
import {PROJECTIONS,Projection,STATE_COUNT,VIEW_MODES,ViewMode,corpusState,decodeAddress,evaluateCorpusModes,projectionPoint,sourceRGB} from './corpusRuntime';
type Props={state:any;onSelect:(coords:{d:number;p:number;r:number;l:number})=>void};
const fmt=(v:number,d=4)=>Number.isFinite(v)?v.toFixed(d):'—';const SIZE=720;function pointFor(address:number,projection:Projection){return projectionPoint(address,projection,SIZE)}
export default function AtlasViewport({state,onSelect}:Props){const baseRef=useRef<HTMLCanvasElement|null>(null),overlayRef=useRef<HTMLCanvasElement|null>(null);const canonical=Number(state?.atlas?.address??0),[cursor,setCursor]=useState(canonical),[playing,setPlaying]=useState(false),[view,setView]=useState<ViewMode>('SOURCE_COLOR'),[projection,setProjection]=useState<Projection>('MANDALA'),[history,setHistory]=useState<number[]>([canonical]);useEffect(()=>{if(!playing){setCursor(canonical);setHistory([canonical])}},[canonical,playing]);const record=useMemo(()=>corpusState(cursor),[cursor]),modeRun=useMemo(()=>evaluateCorpusModes(record),[record]);const move=(a:number)=>{const n=Math.max(0,Math.min(STATE_COUNT-1,Math.floor(a)));setCursor(n);setHistory(h=>[...h,n].slice(-188))};useEffect(()=>{if(!playing)return;const id=window.setInterval(()=>setCursor(a=>{const n=corpusState(a).autoPing.dataNext;setHistory(h=>[...h,n].slice(-188));return n}),260);return()=>window.clearInterval(id)},[playing]);useEffect(()=>{const c=baseRef.current;if(!c)return;const ctx=c.getContext('2d');if(!ctx)return;ctx.clearRect(0,0,SIZE,SIZE);ctx.fillStyle='#04080b';ctx.fillRect(0,0,SIZE,SIZE);if(projection==='LATTICE'){const temp=document.createElement('canvas');temp.width=144;temp.height=144;const t=temp.getContext('2d');if(!t)return;const img=t.createImageData(144,144);for(let a=0;a<STATE_COUNT;a++){const [r,g,b]=sourceRGB(a,view),i=a*4;img.data[i]=r;img.data[i+1]=g;img.data[i+2]=b;img.data[i+3]=235}t.putImageData(img,0,0);ctx.imageSmoothingEnabled=false;ctx.drawImage(temp,0,0,SIZE,SIZE)}else{ctx.globalAlpha=.72;for(let a=0;a<STATE_COUNT;a++){const p=pointFor(a,projection),[r,g,b]=sourceRGB(a,view);ctx.fillStyle=`rgb(${r} ${g} ${b})`;ctx.fillRect(p.x-.4,p.y-.4,.8,.8)}ctx.globalAlpha=1}},[projection,view]);useEffect(()=>{const c=overlayRef.current;if(!c)return;const ctx=c.getContext('2d');if(!ctx)return;ctx.clearRect(0,0,SIZE,SIZE);if(history.length>1){ctx.strokeStyle='rgba(218,177,108,.64)';ctx.lineWidth=1.4;ctx.beginPath();history.forEach((a,i)=>{const p=pointFor(a,projection);i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y)});ctx.stroke()}const mark=(a:number,color:string,r:number)=>{const p=pointFor(a,projection);ctx.beginPath();ctx.arc(p.x,p.y,r,0,Math.PI*2);ctx.fillStyle=color;ctx.fill()};mark(record.autoPing.dataNext,'#4ed4c7',5);mark(record.autoPing.hourglassMirror,'#d7a852',4);mark(record.autoPing.oppositeDomain,'#c34457',4);mark(cursor,'#fff7d2',7)},[cursor,history,projection,record.autoPing.dataNext,record.autoPing.hourglassMirror,record.autoPing.oppositeDomain]);const click=(e:React.MouseEvent<HTMLCanvasElement>)=>{const c=e.currentTarget,r=c.getBoundingClientRect(),x=(e.clientX-r.left)/r.width*SIZE,y=(e.clientY-r.top)/r.height*SIZE;let best=0,dist=Infinity;if(projection==='LATTICE'){const col=Math.max(0,Math.min(143,Math.floor(x/SIZE*144))),row=Math.max(0,Math.min(143,Math.floor(y/SIZE*144)));best=row*144+col}else{for(let a=0;a<STATE_COUNT;a++){const p=pointFor(a,projection),d=(p.x-x)**2+(p.y-y)**2;if(d<dist){dist=d;best=a}}}setPlaying(false);move(best)};return <div className="corpus-explorer"><div className="corpus-toolbar primary"><div className="projection-modes">{PROJECTIONS.map(x=><button key={x} className={projection===x?'active':''} onClick={()=>setProjection(x)}>{x}</button>)}</div><div className="action-row compact"><button onClick={()=>setPlaying(v=>!v)}>{playing?<Pause size={14}/>:<Play size={14}/>} {playing?'Pause':'Traverse source'}</button><button onClick={()=>move(record.autoPing.previous)}><StepBack size={14}/>Back</button><button onClick={()=>move(record.autoPing.dataNext)}><StepForward size={14}/>Source next</button><button onClick={()=>{setPlaying(false);setCursor(canonical);setHistory([canonical])}}><RotateCcw size={14}/>Canonical</button><button className="gold" onClick={()=>{setPlaying(false);onSelect(decodeAddress(cursor))}}><Save size={14}/>Commit</button></div></div><div className="visual-modes data">{VIEW_MODES.map(x=><button key={x} className={view===x?'active':''} onClick={()=>setView(x)}>{x.replace('_',' ')}</button>)}</div><div className="atlas-stage"><canvas ref={baseRef} width={SIZE} height={SIZE}/><canvas className="overlay" ref={overlayRef} width={SIZE} height={SIZE} onClick={click}/><div className="stage-hud top"><span>SOURCE STATE <b>{record.stateId.toLocaleString()}</b> / 20,736</span><span>{record.identity.autoPingState} · Φ {fmt(record.phi.recoverability,3)} · CΩ {fmt(record.metrics.continuity,3)}</span></div></div><div className="data-bands"><section><h4>Predictive engine</h4><div className="mini-grid"><span>CΩ <b>{fmt(record.predict.continuity)}</b></span><span>Λ <b>{fmt(record.predict.burden)}</b></span><span>q <b>{fmt(record.predict.contradiction)}</b></span><span>S <b>{fmt(record.predict.stability)}</b></span></div></section><section><h4>All-modes algebra</h4><div className="mini-grid"><span>executed <b>{modeRun.count}</b></span><span>STAY <b>{modeRun.stay}</b></span><span>TURN <b>{modeRun.turn}</b></span><span>ESCALATE <b>{modeRun.escalate}</b></span></div></section></div></div>}
