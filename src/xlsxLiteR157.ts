import {strToU8,unzipSync,zipSync} from 'fflate';
export type XlsxLiteResult={sheetName:string;rows:(string|number|boolean)[][];formulas:{ref:string;formula:string;value:string|number|boolean}[];truncated:boolean;sheetCount:number};
const td=new TextDecoder(),text=(files:Record<string,Uint8Array>,path:string)=>files[path]?td.decode(files[path]):'',xml=(value:string)=>new DOMParser().parseFromString(value,'application/xml'),esc=(v:any)=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');
const col=(ref:string)=>{const m=/^([A-Z]+)/i.exec(ref);if(!m)return 0;let n=0;for(const ch of m[1].toUpperCase())n=n*26+(ch.charCodeAt(0)-64);return Math.max(0,n-1)},colName=(n:number)=>{let s='',x=n+1;while(x>0){const r=(x-1)%26;s=String.fromCharCode(65+r)+s;x=Math.floor((x-1)/26)}return s},direct=(node:Element,tag:string)=>Array.from(node.children).find(x=>x.localName===tag)||null,valueText=(node:Element,tag:string)=>direct(node,tag)?.textContent||'';
export function readXlsxLite(buffer:ArrayBuffer,maxRows=1000,maxCols=128):XlsxLiteResult{const files=unzipSync(new Uint8Array(buffer)),workbook=xml(text(files,'xl/workbook.xml')),rels=xml(text(files,'xl/_rels/workbook.xml.rels')),relMap=new Map(Array.from(rels.getElementsByTagName('*')).filter(x=>x.localName==='Relationship').map(x=>[x.getAttribute('Id')||'',x.getAttribute('Target')||''])),sheets=Array.from(workbook.getElementsByTagName('*')).filter(x=>x.localName==='sheet'),first=sheets[0];if(!first)throw new Error('Workbook contains no worksheets.');const rid=first.getAttribute('r:id')||first.getAttributeNS('http://schemas.openxmlformats.org/officeDocument/2006/relationships','id')||'';let target=relMap.get(rid)||'worksheets/sheet1.xml';target=target.replace(/^\//,'').replace(/^xl\//,'');const sheet=xml(text(files,'xl/'+target.replace(/^\.\//,'')));if(sheet.documentElement.localName==='parsererror')throw new Error('Worksheet XML could not be parsed.');const sharedDoc=text(files,'xl/sharedStrings.xml'),shared=sharedDoc?Array.from(xml(sharedDoc).getElementsByTagName('*')).filter(x=>x.localName==='si').map(si=>Array.from(si.getElementsByTagName('*')).filter(x=>x.localName==='t').map(x=>x.textContent||'').join('')):[],rows:(string|number|boolean)[][]=[],formulas:XlsxLiteResult['formulas']=[];let truncated=false;for(const rowNode of Array.from(sheet.getElementsByTagName('*')).filter(x=>x.localName==='row')){if(rows.length>=maxRows){truncated=true;break}const out:(string|number|boolean)[]=[];for(const c of Array.from(rowNode.children).filter(x=>x.localName==='c')){const ref=c.getAttribute('r')||`${colName(out.length)}${rows.length+1}`,idx=col(ref);if(idx>=maxCols){truncated=true;continue}const t=c.getAttribute('t')||'',raw=valueText(c,'v'),inline=direct(c,'is'),formula=valueText(c,'f');let value:string|number|boolean='';if(t==='s')value=shared[Number(raw)]??'';else if(t==='inlineStr')value=inline?Array.from(inline.getElementsByTagName('*')).filter(x=>x.localName==='t').map(x=>x.textContent||'').join(''):'';else if(t==='b')value=raw==='1';else if(t==='str')value=raw;else if(raw!==''&&Number.isFinite(Number(raw)))value=Number(raw);else value=raw;while(out.length<idx)out.push('');out[idx]=value;if(formula)formulas.push({ref,formula,value})}rows.push(out)}return{sheetName:first.getAttribute('name')||'Sheet1',rows,formulas,truncated,sheetCount:sheets.length}}
export function writeXlsxLite(rows:(string|number|boolean)[][],sheetName='OMEGA'):Uint8Array{const safeRows=rows.slice(0,20000).map(r=>r.slice(0,256)),sheet=esc(String(sheetName||'OMEGA').slice(0,31).replace(/[\\/?*\[\]:]/g,'_')),rowXml=safeRows.map((r,ri)=>`<row r="${ri+1}">${r.map((v,ci)=>{const ref=`${colName(ci)}${ri+1}`;if(typeof v==='number'&&Number.isFinite(v))return`<c r="${ref}"><v>${v}</v></c>`;if(typeof v==='boolean')return`<c r="${ref}" t="b"><v>${v?1:0}</v></c>`;return`<c r="${ref}" t="inlineStr"><is><t xml:space="preserve">${esc(v)}</t></is></c>`}).join('')}</row>`).join(''),worksheet=`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>${rowXml}</sheetData></worksheet>`,workbook=`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets><sheet name="${sheet}" sheetId="1" r:id="rId1"/></sheets></workbook>`,workbookRels=`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet1.xml"/><Relationship Id="rId2" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles" Target="styles.xml"/></Relationships>`,rootRels=`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>`,styles=`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><styleSheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><fonts count="1"><font><sz val="11"/><name val="Calibri"/></font></fonts><fills count="1"><fill><patternFill patternType="none"/></fill></fills><borders count="1"><border/></borders><cellStyleXfs count="1"><xf/></cellStyleXfs><cellXfs count="1"><xf xfId="0"/></cellXfs></styleSheet>`,types=`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/><Override PartName="/xl/worksheets/sheet1.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/><Override PartName="/xl/styles.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml"/></Types>`;return zipSync({'[Content_Types].xml':strToU8(types),'_rels/.rels':strToU8(rootRels),'xl/workbook.xml':strToU8(workbook),'xl/_rels/workbook.xml.rels':strToU8(workbookRels),'xl/worksheets/sheet1.xml':strToU8(worksheet),'xl/styles.xml':strToU8(styles)},{level:6})}
export function downloadXlsxLite(name:string,rows:(string|number|boolean)[][],sheet='OMEGA'){const bytes=writeXlsxLite(rows,sheet),url=URL.createObjectURL(new Blob([bytes],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'})),a=document.createElement('a');a.href=url;a.download=name.endsWith('.xlsx')?name:`${name}.xlsx`;a.click();setTimeout(()=>URL.revokeObjectURL(url),800)}
