import {corpusState,decodeAddress,encodeAddress,STATE_COUNT} from './corpusRuntime';export const ATLAS_CALCULATOR_SOURCE={name:'Atlas Calculator Instrument',historicalAxes:'12×12×12 Domain/Phase/Regulation',historicalFormula:'packet₁=((D−1)×144)+((P−1)×12)+R',historicalRange:'1…1728',currentFormula:'address₀=1728(D−1)+144(P−1)+12(R−1)+(L−1)',currentRange:'0…20735 / stateId 1…20736',boundary:'The recovered wheel is a 1–1728 D/P/R selector. B058 binds Layer L as the fourth 12-state axis without rewriting the historical formula.'} as const;const axis=(n:number)=>Math.max(1,Math.min(12,Math.floor(Number(n)||1)));export function historicalPacket1(D:number,P:number,R:number){const d=axis(D),p=axis(P),r=axis(R);return(d-1)*144+(p-1)*12+r}export function currentAtlasAddress(D:number,P:number,R:number,L:number){return encodeAddress(axis(D)-1,axis(P)-1,axis(R)-1,axis(L)-1)}export function calculateAtlasPacket(D:number,P:number,R:number,L:number){const d=axis(D),p=axis(P),r=axis(R),l=axis(L),historicalPacket=historicalPacket1(d,p,r),address=currentAtlasAddress(d,p,r,l),record=corpusState(address);return{D:d,P:p,R:r,L:l,historicalPacket,address,stateId:address+1,labels:{domain:record.predict.domain,phase:record.predict.phase,regulation:record.predict.regulation,layer:record.predict.layer},metrics:record.metrics,nextAddress:record.autoPing.dataNext,nextStateId:record.autoPing.dataNext+1}}export function validateAtlasCalculator(){let exact=true,count=0;const seen=new Set<number>();for(let d=1;d<=12;d++)for(let p=1;p<=12;p++)for(let r=1;r<=12;r++){const base=historicalPacket1(d,p,r);if(base<1||base>1728)exact=false;for(let l=1;l<=12;l++){const a=currentAtlasAddress(d,p,r,l),x=decodeAddress(a);seen.add(a);count++;if(a<0||a>=STATE_COUNT||x.d!==d-1||x.p!==p-1||x.r!==r-1||x.l!==l-1)exact=false}}return{passed:exact&&count===STATE_COUNT&&seen.size===STATE_COUNT,count,unique:seen.size,historicalPackets:1728,boundary:ATLAS_CALCULATOR_SOURCE.boundary}}