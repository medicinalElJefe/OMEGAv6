import {Component,Suspense,lazy,useState,type ErrorInfo,type ReactNode} from 'react';
import {GitBranch,Home,ShieldCheck,X} from 'lucide-react';
import './index.css';
import './workstation.css';
import './surpass.css';
import './surfacePolish.css';
import {RUNTIME_IDENTITY} from './runtimeIdentity';
import OmegaHome from './OmegaHome';
const OmegaWorkstation=lazy(()=>import('./OmegaSurpassShell'));
const WovenBuildOutPanel=lazy(()=>import('./WovenBuildOutPanel'));
const SovereignOperationsPanel=lazy(()=>import('./SovereignOperationsPanel'));
type BoundaryState={error:string};
class AppBoundary extends Component<{children:ReactNode},BoundaryState>{state:BoundaryState={error:''};static getDerivedStateFromError(error:unknown){return{error:error instanceof Error?error.message:String(error)}}componentDidCatch(error:unknown,info:ErrorInfo){console.error('OMEGA_APP_BOUNDARY',error,info.componentStack)}render(){if(this.state.error)return <div className='boot'><b>OMEGA {RUNTIME_IDENTITY.hostedBuild} · STARTUP ERROR</b><span>{this.state.error}</span><button className='gold' onClick={()=>window.location.reload()}>Reload OMEGA</button></div>;return this.props.children}}
function App(){const[home,setHome]=useState(true),[buildOut,setBuildOut]=useState(false),[ops,setOps]=useState(false),[addressTick,setAddressTick]=useState(0);const address=()=>{void addressTick;const n=Number(localStorage.getItem('omega.v6.address')||11498);return Number.isFinite(n)?Math.max(0,Math.min(20735,Math.floor(n))):11498};const commitAddress=(n:number)=>{const next=Math.max(0,Math.min(20735,Math.floor(n)));localStorage.setItem('omega.v6.address',String(next));setAddressTick(x=>x+1);window.dispatchEvent(new StorageEvent('storage',{key:'omega.v6.address',newValue:String(next)}))};const navigate=(name:string)=>{localStorage.setItem('omega.v6.panel',JSON.stringify(name));setBuildOut(false);setOps(false);setHome(false);window.dispatchEvent(new StorageEvent('storage',{key:'omega.v6.panel',newValue:JSON.stringify(name)}))};return <AppBoundary>{home?<OmegaHome onEnter={navigate}/>:<Suspense fallback={<div className='boot'><b>OMEGA {RUNTIME_IDENTITY.hostedBuild} · {RUNTIME_IDENTITY.runtimeContract}</b><span>Starting restored + surpass sovereign workstation…</span></div>}><OmegaWorkstation/><button className='omega-home-launch' onClick={()=>{setBuildOut(false);setOps(false);setHome(true)}} aria-label='Return to OMEGA home'><Home/></button><button className='woven-buildout-launch' onClick={()=>{setOps(false);setBuildOut(true)}} aria-label='Open Build Out mode'><GitBranch/>BUILD OUT</button><button className='sovereign-ops-launch' onClick={()=>{setBuildOut(false);setOps(true)}} aria-label='Open Sovereign Operations'><ShieldCheck/>OPS+</button>{buildOut&&<div className='woven-buildout-overlay'><header><div><span>ALL MODES · BUILD OUT</span><b>Woven Continuity Control Plane</b></div><button onClick={()=>setBuildOut(false)} aria-label='Close Build Out'><X/></button></header><main><WovenBuildOutPanel address={address()} onNavigate={navigate}/></main></div>}{ops&&<div className='woven-buildout-overlay'><header><div><span>DRIVE AUTHORITY · PORTABLE OPERATIONS</span><b>Sovereign Operations+</b></div><button onClick={()=>setOps(false)} aria-label='Close Sovereign Operations'><X/></button></header><main><SovereignOperationsPanel address={address()} onCommit={commitAddress}/></main></div>}</Suspense>}</AppBoundary>}
export default App;
