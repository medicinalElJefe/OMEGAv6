import {useEffect,useMemo,useRef,useState} from 'react';
import {Activity,Atom,CloudSun,Globe2,Layers3,Pause,Play,Radio,RefreshCw,Satellite,ShieldCheck,Waypoints} from 'lucide-react';
import {feature} from 'topojson-client';
import land110 from 'world-atlas/land-110m.json';
import {api} from './platformAdapter';
import {corpusState,decodeAddress,STATE_COUNT} from './corpusRuntime';
import {compileSourceTraversal} from './sourceBackedModeRuntimeR21';
import './omegaEarthRelativitySphereR121.css';

type Props={address:number;onAddress?:(address:number)=>void};
type SphereLens='WHOLE'|'CUTAWAY'|'EVIDENCE'|'CANON'|'SPACE';
type Coverage={id:string;label:string;state:string;lastModified?:string|null};
const LAND:any=feature(land110 as any,(land110 as any).objects.land);
const EARTH_RADIUS_KM=6371;
const EARTH_ROTATION_RAD_S=7.292115e-5;
const SURFACE_G=9.80665;
const SHELLS=[
 {name:'INNER CORE',radiusKm:1221.5,kind:'REFERENCE'},
 {name:'OUTER CORE',radiusKm:3480,kind:'REFERENCE'},
 {name:'LOWER MANTLE',radiusKm:5711,kind:'REFERENCE'},
 {name:'TRANSITION 410 km',radiusKm:5961,kind:'REFERENCE'},
 {name:'SURFACE',radiusKm:EARTH_RADIUS_KM,kind:'REFERENCE'}
] as const;
const ATMOSPHERE=[
 {name:'TROPOSPHERE',altKm:12},
 {name:'STRATOSPHERE',altKm:50},
 {name:'MESOSPHERE',altKm:85},
 {name:'THERMOSPHERE REF',altKm:600},
 {name:'EXOSPHERE DISPLAY CAP',altKm:10000}
] as const;
const clamp=(n:number,a:number,b:number)=>Math.max(a,Math.min(b,n));
const wrapLon=(n:number)=>((n+540)%360)-180;
const rad=(d:number)=>d*Math.PI/180;
const deg=(r:number)=>r*180/Math.PI;
const fmt=(v:any,d=1)=>Number.isFinite(Number(v))?Number(v).toFixed(d):'—';
const rgba=(hex:string,a:number)=>{const h=hex.replace('#',''),n=parseInt(h.length===3?h.split('').map(x=>x+x).join(''):h,16);return `rgba(${(n>>16)&255},${(n>>8)&255},${n&255},${a})`};
const atlasAddress=(d:number,p:number,r:number,l:number)=>((((d*12)+p)*12+r)*12+l);
function mappedLatLon(address:number){const c=decodeAddress(address);return{lat:-90+(c.d+.5)/12*180,lon:-180+(c.p*12+c.r+.5)/144*360,coords:c}}
function project(lat:number,lon:number,rotation:number,cx:number,cy:number,R:number,radial=1){const la=rad(lat),lo=rad(wrapLon(lon-rotation)),x=R*radial*Math.cos(la)*Math.sin(lo),y=-R*radial*Math.sin(la),z=Math.cos(la)*Math.cos(lo);return{x:cx+x,y:cy+y,front:z>=0,z}}
function drawLand(ctx:CanvasRenderingContext2D,rotation:number,cx:number,cy:number,R:number){const polys=LAND.geometry?.type==='MultiPolygon'?LAND.geometry.coordinates:LAND.geometry?.type==='Polygon'?[LAND.geometry.coordinates]:[];ctx.strokeStyle='rgba(119,219,201,.78)';ctx.lineWidth=1.05;for(const poly of polys){for(const ring of poly){ctx.beginPath();let open=false;for(const pt of ring){const p=project(pt[1],pt[0],rotation,cx,cy,R);if(!p.front){open=false;continue}if(!open){ctx.moveTo(p.x,p.y);open=true}else ctx.lineTo(p.x,p.y)}ctx.stroke()}}}
function drawArrow(ctx:CanvasRenderingContext2D,x1:number,y1:number,x2:number,y2:number){const a=Math.atan2(y2-y1,x2-x1);ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.lineTo(x2-Math.cos(a-.45)*7,y2-Math.sin(a-.45)*7);ctx.moveTo(x2,y2);ctx.lineTo(x2-Math.cos(a+.45)*7,y2-Math.sin(a+.45)*7);ctx.stroke()}

export default function OmegaEarthRelativitySphereR121({address,onAddress}:Props){
 const canvas=useRef<HTMLCanvasElement|null>(null),drag=useRef<{x:number;rotation:number}|null>(null);
 const[lens,setLens]=useState<SphereLens>('WHOLE'),[running,setRunning]=useState(true),[freeRotation,setFreeRotation]=useState(0),[evidence,setEvidence]=useState<any>(null),[catalog,setCatalog]=useState<Coverage[]>([]),[coverage,setCoverage]=useState('G19-FD'),[busy,setBusy]=useState(false),[error,setError]=useState('');
 const mapped=useMemo(()=>mappedLatLon(address),[address]),record=useMemo(()=>corpusState(address),[address]),route=useMemo(()=>compileSourceTraversal(address,36),[address]);
 const rotation=wrapLon(freeRotation+(running?Date.now()/240000:0));
 const load=async()=>{setBusy(true);setError('');try{const[e,c]=await Promise.all([api.get<any>(`/api/earth/evidence?lat=${mapped.lat.toFixed(5)}&lon=${mapped.lon.toFixed(5)}`),api.get<any>('/api/earth/noaa/catalog')]);setEvidence(e.data||null);const rows=(c.data?.coverages||[]) as Coverage[];setCatalog(rows);if(rows.length&&!rows.some(x=>x.id===coverage))setCoverage(rows[0].id)}catch(x:any){setError(x?.message||'Returned Earth evidence is unavailable.')}finally{setBusy(false)}};
 useEffect(()=>{void load()},[address]);
 useEffect(()=>{if(!running)return;let raf=0;const tick=()=>{setFreeRotation(v=>v+0.018);raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);return()=>cancelAnimationFrame(raf)},[running]);
 useEffect(()=>{const c=canvas.current;if(!c)return;let raf=0,alive=true;const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const draw=(time=0)=>{if(!alive)return;const rect=c.getBoundingClientRect(),dpr=Math.min(2,window.devicePixelRatio||1),W=Math.max(360,Math.round(rect.width)),H=Math.max(520,Math.round(rect.height));if(c.width!==Math.round(W*dpr)||c.height!==Math.round(H*dpr)){c.width=Math.round(W*dpr);c.height=Math.round(H*dpr)}const ctx=c.getContext('2d');if(!ctx)return;ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,W,H);const cx=W*.5,cy=H*.48,R=Math.min(W*.29,H*.31),t=time*.001,rot=wrapLon(freeRotation+(running&&!reduced?t*2.6:0));
  const bg=ctx.createRadialGradient(cx,cy,R*.1,cx,cy,R*2.1);bg.addColorStop(0,'rgba(18,54,60,.28)');bg.addColorStop(.48,'rgba(3,17,22,.72)');bg.addColorStop(1,'rgba(1,5,8,0)');ctx.fillStyle=bg;ctx.fillRect(0,0,W,H);
  ctx.save();ctx.translate(cx,cy);ctx.strokeStyle='rgba(108,178,170,.09)';ctx.lineWidth=.8;for(let rr=R*.45;rr<=R*1.72;rr+=R*.18){ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke()}for(let a=0;a<Math.PI*2;a+=Math.PI/12){ctx.beginPath();ctx.moveTo(Math.cos(a)*R*.42,Math.sin(a)*R*.42);ctx.lineTo(Math.cos(a)*R*1.72,Math.sin(a)*R*1.72);ctx.stroke()}ctx.restore();
  const outerScale=[1.045,1.075,1.105,1.18,1.36];ATMOSPHERE.forEach((s,i)=>{ctx.strokeStyle=i<3?'rgba(94,188,201,.13)':'rgba(144,120,185,.12)';ctx.lineWidth=1;ctx.setLineDash(i===4?[4,8]:[]);ctx.beginPath();ctx.arc(cx,cy,R*outerScale[i],0,Math.PI*2);ctx.stroke()});ctx.setLineDash([]);
  if(lens==='SPACE'||lens==='WHOLE'){ctx.save();ctx.strokeStyle='rgba(94,173,196,.18)';ctx.lineWidth=1.2;ctx.beginPath();ctx.ellipse(cx+R*.42,cy,R*1.62,R*.78,0,0,Math.PI*2);ctx.stroke();ctx.strokeStyle='rgba(221,183,106,.18)';ctx.beginPath();ctx.ellipse(cx,cy,R*1.52,R*.56,rad(-18),0,Math.PI*2);ctx.stroke();ctx.restore()}
  const globe=ctx.createRadialGradient(cx-R*.32,cy-R*.38,R*.06,cx,cy,R);globe.addColorStop(0,'#1c5260');globe.addColorStop(.52,'#0a2c38');globe.addColorStop(1,'#021018');ctx.fillStyle=globe;ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.fill();
  ctx.save();ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.clip();ctx.strokeStyle='rgba(126,190,198,.11)';ctx.lineWidth=.7;for(let lat=-60;lat<=60;lat+=30){ctx.beginPath();let first=true;for(let lon=-180;lon<=180;lon+=3){const p=project(lat,lon,rot,cx,cy,R);if(!p.front){first=true;continue}if(first){ctx.moveTo(p.x,p.y);first=false}else ctx.lineTo(p.x,p.y)}ctx.stroke()}for(let lon=-150;lon<=180;lon+=30){ctx.beginPath();let first=true;for(let lat=-90;lat<=90;lat+=3){const p=project(lat,lon,rot,cx,cy,R);if(!p.front){first=true;continue}if(first){ctx.moveTo(p.x,p.y);first=false}else ctx.lineTo(p.x,p.y)}ctx.stroke()}drawLand(ctx,rot,cx,cy,R);ctx.restore();
  if(lens==='CUTAWAY'||lens==='WHOLE'){const a0=-.72,a1=.72;ctx.save();ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,R,a0,a1);ctx.closePath();ctx.clip();const layers=[{r:1,c:'#4b5657'},{r:5961/EARTH_RADIUS_KM,c:'#6b5847'},{r:5711/EARTH_RADIUS_KM,c:'#4f493f'},{r:3480/EARTH_RADIUS_KM,c:'#8a5b35'},{r:1221.5/EARTH_RADIUS_KM,c:'#d2a45f'}];for(const layer of layers){ctx.fillStyle=layer.c;ctx.beginPath();ctx.arc(cx,cy,R*layer.r,0,Math.PI*2);ctx.fill()}ctx.strokeStyle='rgba(255,236,192,.42)';ctx.lineWidth=1;for(const layer of layers){ctx.beginPath();ctx.arc(cx,cy,R*layer.r,a0,a1);ctx.stroke()}ctx.restore();ctx.strokeStyle='rgba(232,203,137,.55)';ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(a0)*R,cy+Math.sin(a0)*R);ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(a1)*R,cy+Math.sin(a1)*R);ctx.stroke()}
  if(lens==='CANON'||lens==='WHOLE'){ctx.save();ctx.beginPath();ctx.arc(cx,cy,R*1.13,0,Math.PI*2);ctx.clip();for(let i=0;i<STATE_COUNT;i++){const l=i%12,r=Math.floor(i/12)%12,p=Math.floor(i/144)%12,d=Math.floor(i/1728)%12,lat=-90+(d+.5)/12*180,lon=-180+(p*12+r+.5)/144*360,radial=1.012+(l-5.5)*.0065,pt=project(lat,lon,rot,cx,cy,R,radial);if(!pt.front)continue;const current=i===address,next=i===record.autoPing.dataNext,previous=i===record.autoPing.previous;if(current||next||previous){ctx.fillStyle=current?'rgba(100,229,207,.98)':next?'rgba(227,188,110,.92)':'rgba(148,164,163,.8)';ctx.beginPath();ctx.arc(pt.x,pt.y,current?4.8:3.2,0,Math.PI*2);ctx.fill()}else if(i%3===0){ctx.fillStyle=`rgba(104,198,187,${.035+.035*(l/11)})`;ctx.fillRect(pt.x,pt.y,.8,.8)}}ctx.restore();ctx.strokeStyle='rgba(225,189,113,.62)';ctx.lineWidth=1.4;ctx.setLineDash([5,7]);ctx.beginPath();route.path.forEach((x,i)=>{const m=mappedLatLon(x.address),p=project(m.lat,m.lon,rot,cx,cy,R,1.09);if(!p.front)return;i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y)});ctx.stroke();ctx.setLineDash([])}
  const target=project(mapped.lat,mapped.lon,rot,cx,cy,R,1.018);if(target.front){ctx.fillStyle='rgba(249,226,169,.98)';ctx.beginPath();ctx.arc(target.x,target.y,5.2,0,Math.PI*2);ctx.fill();ctx.strokeStyle='rgba(249,226,169,.42)';ctx.beginPath();ctx.arc(target.x,target.y,10+2*Math.sin(t*2),0,Math.PI*2);ctx.stroke()}
  if((lens==='EVIDENCE'||lens==='WHOLE')&&evidence&&target.front){const wind=clamp(Number(evidence?.localConditions?.windKph||0)/100,0,1),dir=rad(Number(evidence?.localConditions?.windDirectionDeg||45)),mag=clamp((Number(evidence?.seismic?.maxMagnitude||0)-1)/7,0,1),kp=clamp(Number(evidence?.spaceWeather?.kp||0)/9,0,1);if(wind>.02){ctx.strokeStyle=`rgba(85,205,239,${.48+.35*wind})`;ctx.lineWidth=1.5+3*wind;drawArrow(ctx,target.x,target.y,target.x+Math.sin(dir)*(28+60*wind),target.y-Math.cos(dir)*(28+60*wind))}if(mag>.02){ctx.strokeStyle=`rgba(224,112,82,${.3+.5*mag})`;ctx.lineWidth=1.2;for(let j=0;j<3;j++){ctx.beginPath();ctx.arc(target.x,target.y,12+j*12+Math.sin(t*2+j)*4*mag,0,Math.PI*2);ctx.stroke()}}if(kp>.04){ctx.strokeStyle=`rgba(95,226,157,${.18+.42*kp})`;ctx.lineWidth=1.2+4*kp;for(const lat of [67,-67]){ctx.beginPath();let first=true;for(let lon=-180;lon<=180;lon+=4){const p=project(lat,lon,rot,cx,cy,R,1.028);if(!p.front){first=true;continue}if(first){ctx.moveTo(p.x,p.y);first=false}else ctx.lineTo(p.x,p.y)}ctx.stroke()}}}
  ctx.strokeStyle='rgba(100,214,195,.42)';ctx.lineWidth=1.4;ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.stroke();
  if(lens==='WHOLE'||lens==='SPACE'){ctx.strokeStyle='rgba(142,188,184,.24)';ctx.lineWidth=1;for(let i=0;i<12;i++){const a=i/12*Math.PI*2,x1=cx+Math.cos(a)*R*1.55,y1=cy+Math.sin(a)*R*1.55,x2=cx+Math.cos(a)*R*1.39,y2=cy+Math.sin(a)*R*1.39;drawArrow(ctx,x1,y1,x2,y2)}}
  ctx.font='10px ui-monospace,monospace';ctx.fillStyle='rgba(217,235,231,.76)';ctx.fillText(`EARTH REFERENCE R=${EARTH_RADIUS_KM.toLocaleString()} km · Ω=${EARTH_ROTATION_RAD_S.toExponential(7)} rad/s · g₀=${SURFACE_G} m/s²`,14,20);ctx.fillStyle='rgba(226,190,113,.86)';ctx.fillText(`OMEGA STATE ${record.stateId} · D${mapped.coords.d+1} P${mapped.coords.p+1} R${mapped.coords.r+1} L${mapped.coords.l+1} · CANONICAL RADIAL LAYERS ARE REPRESENTATIONAL`,14,H-18);
  if(!reduced)raf=requestAnimationFrame(draw)};raf=requestAnimationFrame(draw);return()=>{alive=false;cancelAnimationFrame(raf)}},[address,record,route,mapped,evidence,lens,freeRotation,running]);
 const pointerDown=(e:React.PointerEvent<HTMLCanvasElement>)=>{drag.current={x:e.clientX,rotation:freeRotation};setRunning(false);e.currentTarget.setPointerCapture(e.pointerId)},pointerMove=(e:React.PointerEvent<HTMLCanvasElement>)=>{if(!drag.current)return;setFreeRotation(wrapLon(drag.current.rotation-(e.clientX-drag.current.x)*.35))},pointerUp=()=>{drag.current=null};
 const choose=(e:React.DoubleClickEvent<HTMLCanvasElement>)=>{if(!onAddress)return;const rect=e.currentTarget.getBoundingClientRect(),W=rect.width,H=rect.height,cx=W*.5,cy=H*.48,R=Math.min(W*.29,H*.31),nx=(e.clientX-rect.left-cx)/R,ny=(e.clientY-rect.top-cy)/R;if(nx*nx+ny*ny>1)return;const z=Math.sqrt(Math.max(0,1-nx*nx-ny*ny)),lat=deg(Math.asin(clamp(-ny,-1,1))),lon=wrapLon(deg(Math.atan2(nx,z))+freeRotation),d=clamp(Math.floor((lat+90)/180*12),0,11),lonBin=clamp(Math.floor((lon+180)/360*144),0,143),p=Math.floor(lonBin/12),r=lonBin%12,l=mapped.coords.l;onAddress(atlasAddress(d,p,r,l))};
 const currentCoverage=catalog.find(x=>x.id===coverage)||catalog[0];
 return <section className='r121-sphere' data-truth-boundary='REFERENCE OBSERVED REPRESENTATIONAL'>
  <header className='r121-sphere-head'><div><span>R121 · WHOLE-SPHERE RELATIVITY INSTRUMENT</span><b>Core → surface → atmosphere → orbital context → canonical field</b><small>Physical reference geometry, returned observations, and OMEGA model coordinates stay visibly separated.</small></div><div><button onClick={()=>setRunning(v=>!v)}>{running?<Pause/>:<Play/>}{running?'Pause':'Motion'}</button><button onClick={()=>void load()} disabled={busy}><RefreshCw className={busy?'spin':''}/>{busy?'Refreshing':'Refresh evidence'}</button></div></header>
  <nav className='r121-sphere-lenses' aria-label='Whole sphere lenses'>{(['WHOLE','CUTAWAY','EVIDENCE','CANON','SPACE'] as SphereLens[]).map(x=><button key={x} className={lens===x?'active':''} onClick={()=>setLens(x)}>{x}</button>)}</nav>
  <div className='r121-sphere-body'>
   <div className='r121-sphere-stage'><canvas ref={canvas} onPointerDown={pointerDown} onPointerMove={pointerMove} onPointerUp={pointerUp} onPointerCancel={pointerUp} onDoubleClick={choose} aria-label='Interactive whole-sphere Earth relativity instrument'/><div className='r121-sphere-hint'>DRAG ROTATION · DOUBLE-CLICK SURFACE TO MOVE CANONICAL ADDRESS</div></div>
   <aside className='r121-sphere-telemetry'>
    <section><header><Globe2/><span><b>EARTH REFERENCE</b><small>mean-radius shell geometry</small></span></header><div className='r121-shell-list'>{SHELLS.map(s=><div key={s.name}><span>{s.name}</span><b>{s.radiusKm.toLocaleString()} km</b></div>)}</div></section>
    <section><header><Activity/><span><b>RETURNED OBSERVATIONS</b><small>{evidence?.evidenceHash?'evidence hash bound':'not yet bound'}</small></span></header><div className='r121-kpis'><article><span>TEMP</span><b>{fmt(evidence?.localConditions?.temperatureC)}°C</b></article><article><span>WIND</span><b>{fmt(evidence?.localConditions?.windKph)} km/h</b></article><article><span>SEISMIC</span><b>{evidence?.seismic?.count??'—'} / 24h</b></article><article><span>Kp</span><b>{fmt(evidence?.spaceWeather?.kp)}</b></article></div></section>
    <section><header><Atom/><span><b>CANONICAL RELATIVITY</b><small>atlas address resolution · not physical dimensions</small></span></header><div className='r121-kpis'><article><span>CELLS</span><b>{STATE_COUNT.toLocaleString()}</b></article><article><span>CΩ</span><b>{fmt(record.metrics.continuity,3)}</b></article><article><span>Φ</span><b>{fmt(record.metrics.plasticity,3)}</b></article><article><span>NEXT</span><b>{record.autoPing.dataNext+1}</b></article></div><p><Waypoints/>L is rendered as a relative radial atlas layer so whole/part, inner/outer and representation can change frame without being mislabeled as literal geophysical depth.</p></section>
   </aside>
  </div>
  <div className='r121-source-band'><div><ShieldCheck/><span><b>TRUTH BOUNDARY</b><small>REFERENCE = published Earth constants · OBSERVED = returned provider data · REPRESENTATIONAL = OMEGA atlas/calculus projection</small></span></div><div><Layers3/><span><b>OUTER DOMAINS</b><small>Atmosphere/orbit rings are visually expanded because true altitude-to-radius scaling would be unreadable at this viewport.</small></span></div></div>
  <section className='r121-satellite'><header><div><Satellite/><span><b>NOAA / CIRA GEOCOLOR RETURN</b><small>{currentCoverage?.label||'No returned coverage'} · {currentCoverage?.state||'UNVERIFIED'} · {currentCoverage?.lastModified||'no timestamp returned'}</small></span></div>{catalog.length>0&&<select value={currentCoverage?.id||''} onChange={e=>setCoverage(e.target.value)}>{catalog.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select>}</header>{currentCoverage?<img src={`/api/earth/noaa/image?coverage=${encodeURIComponent(currentCoverage.id)}`} alt={`${currentCoverage.label} NOAA GeoColor returned source`}/>:<div className='r121-satellite-empty'><CloudSun/><span>No satellite coverage returned; nothing synthetic substituted.</span></div>}<footer><Radio/>Satellite imagery is evidence context, not a texture fabricated to fill the sphere.</footer></section>
  {error&&<div className='r121-sphere-error'>{error}</div>}
 </section>
}
