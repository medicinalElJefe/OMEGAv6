import {useEffect,useState} from 'react';
import {CheckCircle2,FileCheck2,GitCommitHorizontal,RefreshCw,ShieldCheck} from 'lucide-react';

type Receipt={schema?:string;state?:string;generatedAt?:string;source?:{repository?:string;sha?:string;branch?:string};qa?:{required?:string;upstreamGate?:string};package?:{builder?:string;output?:string;portable?:boolean;appDeploy?:boolean};deployment?:{authority?:string;canonicalUrl?:string;publicWorkerMutationAuthority?:boolean};workflow?:{runId?:string;runNumber?:string;name?:string};truthBoundary?:string;receiptSha256?:string};

export default function GovernedBuildReceiptPanel(){
 const[receipt,setReceipt]=useState<Receipt|null>(null),[live,setLive]=useState<any>(null),[error,setError]=useState('');
 const refresh=async()=>{setError('');try{const[r,s]=await Promise.all([fetch('/omega-build-receipt.json',{cache:'no-store'}),fetch('/api/status',{cache:'no-store'})]);if(!r.ok)throw new Error(`build receipt HTTP ${r.status}`);if(!s.ok)throw new Error(`status HTTP ${s.status}`);setReceipt(await r.json());setLive(await s.json())}catch(e:any){setError(e?.message||String(e))}};
 useEffect(()=>{void refresh()},[]);
 const sha=receipt?.source?.sha||'UNAVAILABLE',short=sha.length>12?sha.slice(0,12):sha;
 return <section className='panel governed-build-receipt'><div className='section-head'><div><p className='overline'>GOVERNED EXTERNAL BUILD SPINE · READ ONLY</p><h2>Source → QA → package → receipt</h2></div><button onClick={()=>void refresh()} aria-label='Refresh governed build receipt'><RefreshCw size={16}/>Refresh</button></div>{error?<div className='boundary'><ShieldCheck size={15}/>RECEIPT UNAVAILABLE · {error}</div>:receipt?<><div className='buildout-score'><div><span>Package state</span><b>{receipt.state||'UNKNOWN'}</b></div><div><span>Source</span><b>{short}</b></div><div><span>QA gate</span><b>{receipt.qa?.upstreamGate||'UNKNOWN'}</b></div><div><span>Builder</span><b>{receipt.package?.builder||'UNKNOWN'}</b></div><div><span>Run</span><b>{receipt.workflow?.runNumber||'—'}</b></div><div><span>Runtime</span><b>{live?.ok===false?'DEGRADED':'RETURNED'}</b></div></div><div className='buildout-lanes'><article><GitCommitHorizontal/><b>EXACT SOURCE</b><p>{receipt.source?.repository} · {sha}</p></article><article><CheckCircle2/><b>QA + PACKAGE</b><p>{receipt.qa?.required}</p></article><article><FileCheck2/><b>RECEIPT SHA-256</b><p>{receipt.receiptSha256||'UNAVAILABLE'}</p></article></div><div className='boundary'><ShieldCheck size={15}/>{receipt.truthBoundary}</div></>:<div className='boundary'><ShieldCheck size={15}/>Loading governed receipt…</div>}</section>
}
