import {useEffect,useMemo,useState} from 'react';
import {CheckCircle2,Cpu,Loader2,PauseCircle,Play,RefreshCw,ShieldCheck,TriangleAlert,Waypoints} from 'lucide-react';
import {api} from './platformAdapter';
import {defaultCommandPlan,validateCommandPlan} from './hybridCommandRuntime';
import {buildFullSystemMissionObjectiveR154,fullSystemStateContextR154,R154_FULL_SYSTEM_CONTRACT} from './fullSystemCompletionR154.js';
import './fullSystemCompletionR154.css';

const ALLOWED=['INDEX','READ_TEXT','SEARCH_TEXT','HASH_TREE','BUILD','TEST','PACKAGE','SUPPORT_BUNDLE','APPLY_PATCH','WRITE_TEXT'];
const terminal=(v:any)=>['COMPLETE','DONE','FAILED','CANCELLED','REPAIR_REQUIRED','HOLD_PROJECT_NOT_FOUND','HOLD_NO_DECLARED_VERIFICATION','HOLD_MAX_CYCLES','HOLD_DEVICE_PROOF','HOLD_PATCH_PROPOSAL','FAILED_PACKAGE'].includes(String(v||'').toUpperCase());
const age=(n:any)=>{const t=Number(n)||0;if(!t)return'never';const s=Math.max(0,Math.round((Date.now()-t)/1000));return s<60?`${s}s ago`:s<3600?`${Math.floor(s/60)}m ago`:`${Math.floor(s/3600)}h ago`};

type Props={onNavigate:(name:string)=>void};
export default function FullSystemCompletionR154({onNavigate}:Props){
 const[hybrid,setHybrid]=useState<any>(null),[mission,setMission]=useState<any>(null),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
 const root=(()=>{try{return String(window.localStorage.getItem('omega:hybrid:projectRoot')||'.').trim()||'.'}catch{return'.'}})();
 const devices=Array.isArray(hybrid?.devices)?hybrid.devices:[],onlineDevices=devices.filter((d:any)=>d?.online&&!d?.revoked),device=onlineDevices[0]||null,online=Boolean(device&&hybrid?.nativeExecutionClaimed===true);
 const missionState=String(mission?.status||'IDLE').toUpperCase(),job=mission?.currentJob||null,jobState=String(job?.status||'').toUpperCase(),active=Boolean(mission&&(['ACTIVE','PAUSED'].includes(missionState)||(!terminal(missionState)&&!terminal(jobState))));
 const familyRows=useMemo(()=>Object.entries(R154_FULL_SYSTEM_CONTRACT.successor.states),[]);
 const refresh=async()=>{try{const[h,m]=await Promise.all([api.get<any>('/api/hybrid/status'),api.get<any>('/api/missions').catch(()=>null)]);setHybrid(h.data||{});const missions=Array.isArray(m?.data?.missions)?m.data.missions:[];const current=mission?.id?missions.find((x:any)=>x.id===mission.id):missions.find((x:any)=>['ACTIVE','PAUSED'].includes(String(x?.status||'').toUpperCase()))||missions[0];if(current)setMission(current)}catch(e:any){setMessage(`Status read failed safely: ${e?.message||String(e)}`)}};
 useEffect(()=>{void refresh();const id=window.setInterval(()=>void refresh(),3500);return()=>window.clearInterval(id)},[mission?.id]);
 const run=async()=>{if(busy||active)return;if(!online||!device){setMessage('Current authenticated PC heartbeat is required. Open Hybrid Link, run the R127 zero-drift connector from the approved root, then return here.');return}setBusy(true);setMessage('');try{const initial=defaultCommandPlan('BUILD','AUTO_BUILD',root),validation=validateCommandPlan(initial,root,[]);if(!validation.passed)throw new Error(validation.errors.join(' ')||'Initial bounded build plan did not validate.');const objective=buildFullSystemMissionObjectiveR154(root);const stateContext=JSON.stringify({...fullSystemStateContextR154(),surface:'FullSystemCompletionR154',device:{id:device.id,name:device.name||null,platform:device.platform||null},approvedRoot:root});const r=await api.post<any>('/api/missions',{objective,threadId:'',stateContext,draft:{schema:'OMEGA_SOVEREIGN_FULL_BUILD_R151',action:'BUILD',profile:'AUTO_BUILD',projectPath:root,instructions:objective,allowedDomains:[],steps:validation.steps},targetDeviceId:device.id,allowedOps:ALLOWED,maxCycles:12,confirmedMission:true});if(!r.data?.mission?.id)throw new Error('No durable mission identity returned; nothing is treated as started.');setMission(r.data.mission);setMessage(`R154 whole-system objective admitted to the inherited R153 adaptive mission engine on ${device.name||device.id}. Execution truth advances only from returned host proof.`)}catch(e:any){setMessage(e?.message||String(e))}finally{setBusy(false)}};
 const control=async(action:'pause'|'resume')=>{if(!mission?.id||busy)return;setBusy(true);try{const r=await api.post<any>(`/api/missions/${encodeURIComponent(mission.id)}/${action}`,{});if(r.data?.mission)setMission(r.data.mission);setMessage(action==='pause'?'Mission paused. No later adaptive cycle will queue until resumed.':'Mission resumed inside the unchanged R154 objective and R153 proof/mutation envelope.')}catch(e:any){setMessage(e?.message||String(e))}finally{setBusy(false)}};
 return <section className={'r154-completion-executor '+(online?'online':'offline')} aria-label='R154 whole-system completion executor'>
  <header><div><span>R154 · COMPLETE THE ACTUAL ONE SYSTEM</span><h3>24 families · 100 systems · 44 routes · one adaptive governed build mission</h3><p>This is not another redesign. It compiles the restored successor ledger, one-system workbook authority, route/capability contracts, R153 returned-proof continuation, Hybrid execution spine and proof/admission laws into one exact completion objective.</p></div><div className='r154-executor-state'>{online?<CheckCircle2/>:<TriangleAlert/>}<span><b>{online?'PC PROOF CURRENT':'PC PROOF REQUIRED'}</b><small>{online?`${device?.name||device?.id} · heartbeat ${age(device?.lastSeen)}`:'Browser/cloud work remains live; native build cannot be fictionalized.'}</small></span></div></header>
  <div className='r154-executor-kpis'><article><b>{R154_FULL_SYSTEM_CONTRACT.successor.implemented}</b><span>implemented successors</span></article><article><b>{R154_FULL_SYSTEM_CONTRACT.successor.truthGated}</b><span>evidence/device gates</span></article><article><b>{R154_FULL_SYSTEM_CONTRACT.successor.restorationDebt}</b><span>successor restoration debt</span></article><article><b>{R154_FULL_SYSTEM_CONTRACT.inventory.routes}</b><span>registered routes</span></article><article><b>{R154_FULL_SYSTEM_CONTRACT.inventory.sourceModes}</b><span>source modes</span></article><article><b>{R154_FULL_SYSTEM_CONTRACT.inventory.canonLenses}</b><span>canon lenses</span></article></div>
  <div className='r154-family-state' aria-label='R154 family successor states'>{familyRows.map(([id,state])=><span key={id} data-state={state as string}><code>{id}</code>{String(state).replaceAll('_',' ')}</span>)}</div>
  <div className='r154-stage-strip'>{R154_FULL_SYSTEM_CONTRACT.stages.map((s:any)=><button key={s.order} onClick={()=>onNavigate(s.route)} title={s.proof}><code>{String(s.order).padStart(2,'0')}</code><span><b>{s.menu}</b><small>{s.goal}</small></span><Waypoints/></button>)}</div>
  <div className='r154-executor-actions'>{!online?<button className='primary' onClick={()=>onNavigate('Hybrid Link')}><Cpu/>Connect / prove this PC first</button>:missionState==='PAUSED'?<button className='primary' onClick={()=>void control('resume')} disabled={busy}><Play/>Resume whole-system mission</button>:active?<button onClick={()=>void control('pause')} disabled={busy}><PauseCircle/>Pause mission</button>:<button className='primary' onClick={()=>void run()} disabled={busy}>{busy?<Loader2 className='spin'/>:<Play/>}RUN COMPLETE 24-FAMILY BUILD</button>}<button onClick={()=>void refresh()} disabled={busy}><RefreshCw/>Refresh proof</button><button onClick={()=>onNavigate('Evidence & Proof')}><ShieldCheck/>Open receipts / proof</button></div>
  {mission&&<div className='r154-mission-receipt'><span><b>{missionState} · {String(mission.id||'').slice(0,18)}</b><small>R153 adaptive · cycle {Number(mission.cycle??mission.cycles??0)}/{Number(mission.maxCycles||12)} · stage {String(mission.stage||'pending')} · job {job?.id?String(job.id).slice(0,16):'pending'} · {jobState||'no returned job yet'}</small></span><code>{job?.returnPacket?.resultFingerprint||mission?.finalResultFingerprint||'R141/R142 return fingerprint pending'}</code></div>}
  {message&&<div className='r154-executor-message'>{message}</div>}
  <footer><ShieldCheck/><span>{R154_FULL_SYSTEM_CONTRACT.completionDefinition}</span><small>{R154_FULL_SYSTEM_CONTRACT.truthBoundary}</small></footer>
 </section>
}
